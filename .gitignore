# ---------------------------------------------------------------------------
# Upstream AudioLDM-training-finetuning ignores, kept verbatim from
# upstream-frozen (702a638d023b008a2d9a45cdf1e1f4fcdc590dfc) so that
# `git diff upstream-frozen` stays minimal and reviewable.
# NOTE: this block ignores *.txt. Keep research inventories/manifests in
# .md/.json/.yaml so they are never silently dropped.
# ---------------------------------------------------------------------------
__pycache__
taming
log
esc50.zip
ESC-50-master
*.wav
ckpt
lightning_logs
mlx_submit_*
job_queue.sh
*.txt
*.cleaned
audiocaps_train.json
dataset
checkpoints
*.tar
condor*
wandb

# agent-kit audioldm research ignores
artifacts/
_external/
datasets/
checkpoints/
wandb/
*.ckpt
*.safetensors
*.pt
*.pth
audioldm-agent-kit-minimal*.zip
.env
.env.*
CLAUDE.local.md
.claude/settings.local.json
__pycache__/
.pytest_cache/
.venv/
.agent-kit-backup-*/
.venv-sa3
.venv-sa3/
data/sa3/
.venv-metrics
.venv-metrics/

# stray lock from ephemeral `uv run --no-project` invocations (project env is poetry.lock-based)
uv.lock

# listening study: audio bundles + unblinding artifacts + tooling venv (never commit to public repo)
.venv-loudness
listening_study/audio/
configs/research/listening_study_assignments_private.json
configs/research/listening_study_bundle_manifest.json
configs/research/listening_deploy_slugs.local.json
public_examples/

# ICASSP manuscript build intermediates & raster previews (keep .tex, .sty, .bst, .pdf, vector figs)
icassp/build/
icassp/figs/*.png
# Overleaf delivery bundle (derivable from the icassp/ sources; not versioned)
icassp/icassp_operating_point_overleaf.zip
