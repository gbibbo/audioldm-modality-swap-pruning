# Claims Matrix

Allowed status values: `unresolved`, `supported`, `rejected`, `exploratory`, `unavailable`.

**Contract: `docs/master_plan_v4.md` (adopted 2026-08-20, DECISION-V4-00).** The v3 RQ-swap
rows (RQ1/RQ2a/RQ2b) are the closed negative result; the v4 rows are RQ1′–RQ4′. Wording of
the RQ-swap negative is decided by D1 (DECISION-V4-05): "not supported" vs "not detectable at
this budget".

| ID | Candidate claim | Required evidence / branch | Status | Evidence paths | Allowed paper wording |
|---|---|---|---|---|---|
| RECOVERY-REVERSAL-V1.1 | Recovery-induced cross-domain ranking reversal: the published post-pruning "recovered" AudioLDM beats pruned-only in-domain (AudioCaps) while losing to it on held-out music, under a controlled common operating point. | Frozen prereg V1 (`8d059f21`) + V1.1 amendment (`6eea0720`); 96×2×3; gate R_AC≥+0.025 ∧ lo95(R_AC)>0 ∧ lo95(I)>0 | rejected | ledger RECOVERY-REVERSAL-V1.1-RESULT; `configs/research/reversal_v1_1_result.json` (sha `cad7c454`); job `reversal-v11-gen-1` (1.262 cr, T4) | **PRIMARY PASS = FALSE — pre-registered NEGATIVE (2026-08-29).** In-domain R_AC = −0.0024, CI95 [−0.0267, 0.0214]: recovered ~matches pruned (C 0.098 vs 0.100; beats it in 44.8% of prompts), does NOT exceed it; dense-gaps ≈ equal (0.106 vs 0.104). Interaction I = +0.092 [0.053, 0.131] is positive (recovered far worse on music, C 0.023 vs 0.117) but the required strict in-domain advantage is absent. Human-CLAP secondary R_AC_HC = +0.028 [−0.012, 0.067] (CI crosses 0; sign disagrees with primary, both inconclusive). **Allowed wording: "the recovery-induced cross-domain ranking reversal is _not supported_ under the controlled same-operating-point experiment."** Do NOT claim operating-point brittleness; no rescue experiment. |
| RECOVERY-METRIC-AUDIT-1 (POST-HOC diagnostic, NOT preregistered) | Is post-pruning "recovery" metric-dependent — restoring the authors' distributional/event metrics (FAD, KL, PANN capture) without restoring text–audio alignment (CLAP)? | Outcome-motivated post-hoc metric-concordance diagnostic on the frozen V1.1 outputs; frozen protocol `529ff6a3`; no gate/SESOI/composite | **not supported (metric-dependence rejected as the explanation)** | ledger RECOVERY-METRIC-AUDIT-1-RESULT; `configs/research/recovery_metric_audit_1_result.json` (sha `358be728`); 0 GPU/0 cr | **POST-HOC, cannot change V1.1 PASS=FALSE.** At the controlled 3.84 s/DDIM50/n=96 operating point, dense ≫ {pruned ≈ recovered} on ALL six axes and **no metric — incl. the authors' own FAD & KL — shows recovered beating pruned** (capture Δ +0.023 [−0.044,+0.088]; KL Δ −0.067 [−0.396,+0.251]; FAD rec 14.70 vs pruned 14.53; FD rec 80.8 vs pruned 78.4; CLAP/HC rec≈pruned). Recovered≈pruned is **metric-invariant**, so Pattern A (metric-axis dependence) is NOT the explanation → **Pattern B: operating-point/inference-recipe dependence** is the leading candidate (Singh's FAD 1.57 measured at 200 steps/10 s). **Allowed wording: "under a controlled short-clip/few-step operating point the recovered checkpoint is statistically indistinguishable from pruned-only across distributional, divergence, event-capture, and text-alignment metrics simultaneously; the published recovery advantage does not transfer off its evaluation recipe."** NOT "the paper is wrong". Absolute FAD/KL NOT comparable to Singh (different op-point). Operating-point experiment NOT yet run. |
| OP-DURATION-DISCRIMINATOR-1 (Arm D; prospective post-V1.1 follow-up) | Is the recovered-vs-pruned ordering temporal-scale-conditional? (only factor changed: duration 3.84s->10.24s) | Frozen protocol `fe2be79f`; 80-ytid subset `ce5fad11`; matched 4-group CLAP; primary J_CLAP MDE~0.065 | **supported (material recovered advantage at 10.24s; strict interaction gate borderline)** | ledger OP-DURATION-DISCRIMINATOR-1-RESULT; `configs/research/op_duration_discriminator_1_result.json` (sha `9c92552a`) + `..._secondary.json`; job `reversal-armd-gen-1` (0.5766 cr, T4) | **FINAL GPU experiment; V1.1 PASS=FALSE untouched.** At 3.84s rec~pru on every axis; at native 10.24s recovered MATERIALLY beats pruned across all six (CLAP R_alt +0.052 [0.009,0.093]>=SESOI; HC J_HC +0.075 [0.012,0.137]; KL R_alt +0.58 [0.20,1.00]; PANN +0.36 [0.11,0.63]; FAD 5.41 vs 12.25; FD 60.2 vs 71.4). Primary J_CLAP +0.044 [-0.001,+0.087] positive but 95% CI marginally includes 0 (strict interaction gate not resolved at a=0.05). **Allowed wording: "the recovered checkpoint's advantage over pruned-only is temporal-scale-conditional -- resolved at the native 10.24s scale, absent at 3.84s; the primary interaction contrast is positive but narrowly misses the 95% gate."** Do NOT claim "restored to dense" (no dense@10.24s); DDIM200 untested (limitation); NO further experiment. |
| RECOVERY-CROSS-SEVERITY-REP-1 (prospective independent replication at severity-2 (1,2,1,1); A′ primary + B′ seam sensitivity) | Does the recovered-vs-pruned CONTEXT dependence (K) + temporal-scale interaction (J) replicate at a stronger pruning severity, and does the severity-1 native-positive/music-negative sign-pattern reproduce? | Frozen protocol `19c50cc3`; independent AudioCaps-192 (`4da90661`) + music-64 (`f5a26fbe`); K_A gate lo95>0, J_A gate lo95>0, sign-pattern conjunction H_native∧H_music; bootstrap PCG64(20260831) B=10000 | exploratory (CASE C — partial replication) | ledger RECOVERY-CROSS-SEVERITY-REP-1-RESULT + post-result audit (`docs/xsev_postresult_adversarial_audit.md`); `configs/research/xsev_result.json` (sha `02e3bd11`); jobs `reversal-xsev-gen-1`+`xsev-dense-tail-3` (3.6883 cr, T4) | **CASE C — reproduction verified bit-exact (max diff 0.0).** K_A +0.235 [0.197,0.272] PASS and J_A +0.159 [0.131,0.187] PASS (both seam-robust under A′ and B′): context dependence + temporal-scale interaction REPLICATE at the stronger severity. H_native TRUE (R_native +0.244), but **the native-positive/music-negative SIGN-PATTERN does NOT replicate** — R_music +0.009 [−0.013,+0.032], H_music FALSE (no OOD penalty; music = very low absolute CLAP scores with full spread, NOT a floor). Recovery advantage is PRESENT (not absent) at 3.84s and LARGER at 10.24s (= J). **K bundles domain+duration → say "context dependence", NOT "domain interaction"/"OOD penalty" (§7).** Dense control is severity-1 lineage; dense−recovered +0.048 [−0.0004,+0.096] → NO "restored to dense" (§8). Secondaries corroborative only: KL & PANN-captured-label-count CIs exclude 0; FD/FAD descriptive (no CI); HC music −0.037 [−0.068,−0.005] cannot override primary H_music=FALSE. **Allowed wording: "context-dependence and the temporal-scale interaction of recovered-vs-pruned independently replicate at a stronger pruning severity; the specific OOD-music penalty does not, and the recovery advantage is temporal-scale-conditional but present at both scales."** Cross-severity heterogeneity is POST-HOC/exploratory only (§5, confounded). Do NOT claim OOD penalty, domain interaction, restored-to-dense, or a formally-established severity effect. |
| XSEV-MUSIC-NATIVE-1 (prospective design completion; frozen before generation; NO gate) | At severity 2, does the recovery gain on held-out music appear at the native 10.24 s duration? | Protocol `docs/xsev_music_native_1.md` (sha `69caa3d3…`); music-64 × r0 @10.24 s, recovered2 vs pruned2_A, CRN; primary R_music_nat with cluster CI; branches (a)/(b)/(c) | **supported — branch (a)** | ledger XSEV-MUSIC-NATIVE-1-RESULT; `configs/research/xsev_music_native_1_result.json`; job `xsev-music-native-1` (0.4651 cr, T4) | **R_music_nat +0.005 [-0.028, +0.039] (null; W 0.53); J_music -0.004 [-0.043, +0.034]; D_nat +0.239 [+0.195, +0.283].** Allowed wording: "at severity 2 the recovery gain is absent on held-out music at both durations; the large native-duration gain is confined to the fine-tuning domain (matched-duration domain contrast +0.076 at 3.84 s, +0.239 at 10.24 s)." Do NOT say "music penalty" (null, not negative). B′ not generated (credit minimisation; stated as a limitation of this cell). |
| DRAFT3-SENSITIVITY + NATIVE-CROP-ANALYSIS (post-hoc, authorised 2026-09-02) | (B1) Is the duration interaction scale-free? (B2) matched-duration domain contrast sev-2. (B3) Does the short-duration deficit come from generation length or scoring window? | Committed raw per-prompt scores; declared seed namespaces; no gate | exploratory (post-hoc sensitivity / diagnostic) | ledger DRAFT3-SENSITIVITY-RESULT, NATIVE-CROP-ANALYSIS-RESULT; `configs/research/draft3_sensitivity_result.json`, `native_crop_analysis_result.json` | **B1:** win-rate interaction sev-1 +0.200 [+0.063, +0.338], sev-2 +0.146 [+0.073, +0.219] (both resolved; raw-scale sev-1 J stays borderline). **B2:** sev-2 R_short−R_music +0.076 [+0.047, +0.105]. **B3:** first 3.84 s of the native generation carries R_crop sev-1 +0.051 [+0.016, +0.085] (= R_native; R_crop−R_short +0.043 [+0.002, +0.085]) and sev-2 +0.172 [+0.150, +0.194] (R_crop−R_short +0.087, R_nat−R_crop +0.072). Allowed wording: "the short-duration deficit is mostly a property of what the fine-tuned checkpoint generates when asked for a short clip, not of scoring a short excerpt" — labelled post-hoc; no mechanism claim. |
| DRAFT4-DENSE-DURATION-CONTROL + DRAFT4-ROBUSTNESS (post-hoc, Gabriel review request 2026-09-02) | (C1) Does the dense model's duration response on the same prompts explain the interaction? (C2) Do the reported contrasts survive a family-wise correction? (C3) Is J robust to rank/median statistics? (C4) Is the domain null a caption-length artefact? | 80 existing V1.1 dense r0 WAVs re-scored under the frozen 80-item convention + committed raw scores; declared seed namespaces; consistency guards vs frozen points (0.0) | exploratory (post-hoc control / sensitivity) | ledger DRAFT4-DENSE-DURATION-CONTROL, DRAFT4-ROBUSTNESS; `configs/research/draft4_dense_duration_control_result.json`, `draft4_robustness_result.json`; review `docs/review/2026-09-02_manuscript_draft3_review.md` | **C1:** s(dense) +0.150 [+0.100,+0.198] (0.202→0.352, n=80 paired) = s(P) +0.149 at sev-1 (diff −0.001 [−0.056,+0.055]); s(P+FT) +0.193, excess over dense +0.043 [−0.020,+0.109] (not resolved); gap closed 8% @3.84 s vs 52% @10.24 s. Allowed wording: "at 65% pruning the pruned checkpoint responds to duration like the dense model; at 83% pruning s(P)=+0.040 has lost most of that response while s(P+FT)=+0.200 is of dense magnitude (cross-set, descriptive)". Do NOT claim P+FT > dense in duration response (CI includes 0). **C2:** Holm over 13 contrasts — all sev-2 † survive (p<1e-4); sev-1 R_nat (p=0.016) and J (p=0.052) do NOT → sev-1 duration effect = "directionally consistent, established by the sev-2 replication". **C3:** median J +0.051 [+0.012,+0.077] / +0.172 [+0.153,+0.214]; pooled-rank J sev-1 not resolved. **C4:** gain uncorrelated with caption length within AudioCaps (ρ +0.04 [−0.12,+0.18]); music captions 7× longer → "domain bundles content and caption style" (limitation). Batch-composition shift on means ≤0.002. |
| RQ1 (v3, RQ-swap) | Structured pruning causes modality-dependent damage beyond the matched random-pruning null. | v3 M3 Gate A (closed) | rejected | ledger M3A-DIAG-RUN, `artifacts/m3_pilot/m3a_result.json` | **Gate A FAIL (2026-08-20, job `m3a-diag-1`, W=200/K=5/20 masks).** L1's modality-dependent damage `R_mod=0.2298` vs random `0.2279`; matched on `D_gen` (8.171 vs 8.096, interpolated), `Delta_swap=0.0007`, 95 % bootstrap CI [−0.0025, +0.0028] (includes 0), standardized residual 0.459 < 0.50. Pruning does **not** damage one modality more than the other beyond a matched random null. Under v4 this is the **RQ-swap closed negative**; final wording ("not supported" vs "not detectable at this budget") set by D1 (DECISION-V4-05). Reinforces RQ2a. Audited: L1 interpolated in the random cloud, `R_mod` non-degenerate. |
| RQ2a (v3, RQ-swap) | Paired modality adds value beyond faithful text-only Taylor at matched gradient-evaluation budget. | v3 M3 Gate B (closed) | rejected | `docs/m0_baseline_reproduction/l1_pruning_direction_finding.md`, `docs/pilot_protocol.md` (M3B section), `research_pruning/paired_modality/overlap.py`, ledger M3B-SALIENCY-RUN | **Gate B FAIL (2026-08-20, job `m3b-saliency-2`): weighted kept-set overlap 0.9475 (threshold ≤ 0.80), 0 layers ≤ 0.70.** Audio and text Taylor saliencies agree almost completely (mean per-layer Spearman 0.980 vs 0.571 for Taylor-vs-L1, a discriminating control), so the paired criteria collapse onto text-only (P1 vs P2 = 0.948, P2 vs P3 = 0.987). **Rejected at the saliency stage;** M4 screening (M4-SCREEN-FOUND) recorded but not promotable (FAD/FD NaN, 1 seed). Audited real (S_a≠S_t bitwise, FiLM reaches all gated ResBlocks, embeddings differ). **Baseline convention (DECISION-M3B-002/003, amended by DECISION-V4-01):** P0-standard (keep-highest-L1) is now the **primary** baseline; P0-published (Arshdeep's inverted keep-lowest-L1 artefact) is the **reproducibility control** — any comparison to it worded "vs the published L1 artefact", never "vs standard L1". |
| RQ2b (v3, RQ-swap) | Max aggregation adds value beyond paired mean. | v3 M4 P3 vs P2 (closed) | rejected | ledger M3B-SALIENCY-RUN | At the saliency stage P3 (swap-max) and P2 (paired-mean) keep **0.987** of the same channels (job `m3b-saliency-2`), so max aggregation barely differs from mean — a direct consequence of S_a≈S_t. **Rejected at the saliency stage.** |
| RQ1′ | Under structured pruning at fixed budget, per-event semantic loss is heterogeneous beyond a random mask of matched generic damage, and the heterogeneity is stable across pruning criteria. | v4 Gate E (Tier 1; exact rank test `K_rand=20`) + Tier-0 heterogeneity screen | unresolved | `docs/master_plan_v4.md` §6 (Gate E), ledger DECISION-V4-04 | No paper wording yet. Tier 0 provides a heterogeneity **screen only** (no RAND audio → Gate E not evaluable there); Gate E is Tier 1, gated on the CPU power simulation reaching ≥ 80 % power (M4-1b). SEM (event-family vulnerability) is folded into this row. |
| RQ2′ | Per-event loss is predicted by exposure (H-tail), guidance dependence + pruning error in it (H-guidance), or acoustic descriptors (H-acoustic) — pre-registered model comparison; any one or none is acceptable. | v4 Gate M (nested block LRTs; CV tie-break) | unresolved | `docs/master_plan_v4.md` §3, §6 (Gate M) | No paper wording yet. H-acoustic stays in the primary Gate M only if the FineLAP CPU smoke passes (DECISION-V4-06, Q3); otherwise it is sensitivity-only on the single-label subset. |
| RQ3′ | A calibration reweighting motivated by the winning mechanism changes the kept set beyond the calibration-sampling null (Gate B′) and reduces tail-event loss pre-recovery without raising head-event loss (Gate I). | v4 Gate B′ + Gate I (+ P1-placebo control) | unresolved | `docs/master_plan_v4.md` §5–§6 | No paper wording yet. Runs only if a mechanism block wins Gate M; Gate I on the disjoint intervention holdout only, margins `δ_target=+5 pp`, `δ_harm=2 pp`, FAD/FD +5 % rel, KL +0.05 (DECISION-V4-07); holdout stays blinded until §7 params are frozen. |
| RQ4′ | With a fixed parameter-efficient budget, how much event-level loss is restored for the best criterion vs P0-standard, versus Singh et al.'s 1M-step full fine-tune. | v4 M5 recovery (Tier 2) | unavailable | `docs/m0_baseline_reproduction/public_artifact_inventory.md` | **`unavailable` until Tier 2 is funded (DECISION-V4-02: Tier 0 only authorized now).** The recovered full-FT `(1,2,3,1)` checkpoint is proven not public, so any recovery claim is a published-reference comparison, with the caveat that Singh et al.'s fine-tuned model (FAD 1.57) beats their unpruned model (FAD 3.95), i.e. full FT is also AudioCaps domain adaptation. |
| SEM | Specific semantic/event families remain differentially vulnerable. | Folded into RQ1′ (v4) | unresolved | `docs/master_plan_v4.md` §2 | Merged into RQ1′ under v4; not a separate claim. |
| EFF | Reported pruning/recovery efficiency is supported by measured runtime, VRAM, GPU-hours, and storage. | M1/M4/M5 (v4) | unresolved | `artifacts/m0_baseline_reproduction/architecture_check.log` | Parameter counts only are measured so far (U-Net 415.955 M -> 145.674 M, -65.0%; mild budget `(1,2,3,4)` 317.308 M, -23.7 %). Runtime, VRAM and GPU-hours partially measured (`docs/compute_budget.md`); no end-to-end efficiency claim yet. |

No rejected or exploratory claim may be promoted to a headline claim.

## SA3 line (stable-audio-3 modality-swap pruning) — CLOSED 2026-08-26

Distinct sub-line (its own RQ numbering; not the AudioLDM v3/v4 rows above). Closure labels map:
ESTABLISHED→methodological result held; FAILED→`rejected`; NOT TESTED→`unavailable` (NOT RUN, not TODO).
Postmortem: `docs/sa3/sa3_postmortem.md`.

| ID | Candidate claim | Status | Closure label | Evidence paths | Allowed wording |
|---|---|---|---|---|---|
| SA3-RQ1 | Adaptability occupies a distinct internal resource; `I_PT` carries structure beyond pruning damage `D_P`. | rejected | FAILED (closed) | `artifacts/sa3/rq1_reanalysis.json` (`IPT_collapses_onto_DP`), ledger SA3-RQ2VAL-001 | `I_PT` is redundant with `D_P`; no distinct adaptation-resource structure was established. |
| SA3-RQ2 | Single-block LoRAs are algebraically localisable AND functionally useful; `A_tan` predicts real adapter compatibility better than `D_P`. | rejected | FAILED (closed) | `configs/sa3/adapters/control_verdict.json`, `control_taa_scores.json`, ledger SA3-CONTROL-TAA-004 | Controls L_6/L_13 passed field + host-collapse + external observability but FAILED the task-level ΔT_AA gate — a **field/function gap** (measurable, algebraically-localisable internal perturbation with no measurable dense task utility). The `A_tan`-vs-`D_P` prediction check was never reached. |
| SA3-RQ2b | SA3 can learn a dense, functionally-useful adapter (the prerequisite worth preserving under pruning). | rejected | FAILED (closed) | `configs/sa3/adapters/f1_verdict.json`, `f1_taa_scores.json`, ledger SA3-RQ2B-F1-RUN | F1 full-backbone LoRA r16/α16 (all 20 blocks, `max\|lora_B\|=1.197`, 96 train / 1000 steps): ΔT_AA(base)=+0.0118 CI[−0.020,+0.043], ΔT_AA(post)=+0.0230 CI[−0.011,+0.058] — both **upper CI bounds below SESOI 0.075** → incompatible with a SESOI-magnitude effect. Under the frozen standard recipe the adapter failed to produce a functionally relevant paired T_AA uplift on either dense base or dense post. Do NOT generalize to "LoRA does not work on SA3" or "adapter-compatible pruning is false". |
| SA3-STRUCT | `A_tan` and `D_P` show a stable structural ranking difference (δ=2 at k=6, robust N=16→N=32). | exploratory | ESTABLISHED (structural only) | `artifacts/sa3/rq1_reanalysis.json`, `docs/sa3/round2_results.md` | A structural difference exists but was **never linked to real functional adapters**; not promotable. |
| SA3-F2 / ecological / RQ3 | Restricted-support (band) adapters; ecological adapters; A_eco/A_tan-vs-D_P against a qualified control. | unavailable | NOT RUN | — | Gated on an F1 PASS that did not occur. NOT RUN (not TODO). Any revival must first establish an independent adapter/benchmark with demonstrated dense functional uplift BEFORE pruning analysis. |


## ICASSP-2027 Scenario B (deterministic mask-induced adapter transfer) — ACTIVE (DECISION-V4-09+)

| ID | Candidate claim | Required evidence | Status | Evidence | Allowed wording |
|---|---|---|---|---|---|
| SB-Gate0 | The Kim dense-trained LoRA produces a real text-audio alignment uplift on AudioLDM-M-Full (the legacy adapter worth transferring is real). | Gate 0: powered ΔCLAP on the held-out 64-prompt battery, point ≥ SESOI 0.025 AND lower-CI95 > 0. | **established** | ledger GATE0-VERDICT (2026-08-27); `artifacts/icassp_gate0/gate0_verdict.json` md5 62cefa0b; jobs gate0-train-1 (19,400 updates, adapter 84a24a38, e349d69) + gate0-gen-1 | **Gate 0 PASS: significantly positive uplift whose point estimate (ΔCLAP 0.0464) exceeds the pre-defined SESOI; 95% CI [0.0221, 0.0720] (n=64 prompts, B=10,000); base 0.197 → adapter 0.243, consistent across 3 seeds.** Powered transposition of the Kim recipe to M-Full (NOT an exact replication of Kim) under a powered, source-ytid-disjoint held-out battery. (Point exceeds SESOI; CI lower bound 0.022 > 0 as the gate requires — NOT "effect ≥ 0.025 with 95% confidence".) Scorer-pinning reproducibility control (rev 365dea6e) reproduces this verdict EXACTLY (ledger PHENOM-STAT-D). |
| SB-phenom | Under structured pruning, the transferred legacy adapter's benefit is disproportionately damaged beyond generic capacity loss (differential fragility), and standalone recovery does not restore it. | Pre-registered phenomenon falsifier (prereg **v5**): PRIMARY = `p1_recovered`, SECONDARY/mechanistic = `p1_pruned_ema_reconstructed`, each × {±sliced LoRA}; dual gate per severity = standalone non-inferiority (upper-CI95[E]≤0.025) AND **differential fragility on D=ΔCLAP(0)−ΔCLAP(s)** (point≥0.025 AND lower-CI95[D]>0). Single primary endpoint → no MCC. | **refuted (pre-registered negative)** | prereg v5 `phenomenon`; ledger PHENOM-VERDICT (2026-08-27); `artifacts/icassp_gate0/phenomenon_verdict.json` md5 326eb639; job gate0-phenom-1 (768 WAVs, 1.6020 cr) | **PRIMARY p1_recovered: phenomenon FALSE.** Standalone NOT preserved: E point 0.174, CI95 [0.145, 0.204] (≫ 0.025 margin) — the published recovered pruned backbone does not restore standalone text-audio alignment on the held-out battery. Differential fragility IS present (D point 0.0436, CI95 [0.019, 0.069]) but is confounded with a large generic capacity loss, so the dual gate (standalone non-inferiority AND D fragility) is NOT met → descriptive only. SECONDARY p1_pruned_ema_reconstructed: E 0.080 [0.054,0.107], D 0.024 [−0.006,0.054], phenomenon FALSE. Decision statistic = **D** (F deprecated; both F<0 here). Scored with the frozen Option-B convention (192/system seed-once, rev 365dea6e; dense reproduces Gate 0 bit-exact). **STOP-2 pre-registered negative; no rescue/pivot** (amendment §I). |
