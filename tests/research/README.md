# `tests/research/`

CPU tests for research code in this repository.

**There are currently no research tests here.** The master plan records a LoRA
CPU scaffold with passing tests as existing work, but that code lives only on
the author's local machine and has not been recovered into this repository.
Until it is recovered and its tests re-run here, no M1 status may be claimed
from this repository.

`tests/` at the repository root additionally contains upstream AudioLDM tests,
unmodified from `upstream-frozen`.
