# AudioLDM Modality-Swap Pruning - Progress

> Living state document. Keep this compact. The SessionStart hook injects only the bounded state block below. Detailed experimental provenance belongs in `docs/experiment_ledger.md` and milestone artifacts.

## CURRENT STATE

* **🟢 FALSIFIER LAUNCHED — 768-gen (2026-08-27 12:53, ledger PHENOM-SCORING-B). 3 fixes committed + CPU-green.** Gabriel accepted `11e31ee`, authorized the 768-gen falsifier after 3 mechanical fixes. **FIX 1 (budget reconcile, DECISION-V4-13-BUDGET-RECONCILE):** prereg v5 `budget:` block reconciled ADMINISTRATIVELY (owner-reported ~10 cr; Lightning `balance` field labelled STALE; central-chain auth ≤5.0; spent 2.8648; remaining 2.1352; old 5.0/3.0 kept as historical) — NOT scientific (v5 body freeze sha `231b98ed`; file sha `43617a6b`; B8 8/8). **FIX 2 (primary first):** `run_phenomenon_gen.sh` generates `p1_recovered` before `p1_pruned_ema_reconstructed`. **FIX 3 (scoring convention, Option B chosen by Gabriel):** CLAP-fused (transformers 4.30.2) has a BATCH-LEVEL stochastic `is_longer` selection for short-audio batches (audio embeddings ARE batch/order-sensitive: max|Δemb| ~0.08 re-chunked; TEXT bit-exact 0.0) — so per-item seeding would redefine the scorer. FROZEN convention = **ONE 192-item scorer call PER SYSTEM**, order (prompt,rep), `np.random.seed(20260826)` reset once per system, rev `365dea6e` (`score_groups`; nuisance paired by position across systems). **Bit-exact control PASS:** re-scored dense OFF/ON per-system → per-item cosines 0.0, mean cosines 0.0, ΔCLAP point/lo/hi 0.0 vs frozen, PASS unchanged (historical Gate-0 NOT rewritten). Launch: T4 on-demand FP32, sliced adapter `5cc0a79a`, watchdog max-cost 2.05/max-min 150; dense 384 reused byte-for-byte. On completion: retrieve 768 WAVs → `phenomenon_score_verdict.py` (per-system scoring, D-gate, primary=recovered) → STOP + report.
* **✅ GATE 0 ACCEPTED (Gabriel, 2026-08-27) + PRE-PHENOMENON-DATA PREP COMPLETE (ledger PHENOM-STAT-D, 11:35). CPU-only, 0 cr, GPU NOT launched; falsifier still NOT authorized.** Paper-facing wording fixed: "significantly positive uplift whose point estimate exceeds the pre-defined SESOI" (NOT "effect ≥ 0.025 with 95% confidence"); "powered transposition of the Kim recipe to M-Full" (NOT "exact replication"). All Gate-0 artifacts preserved byte-for-byte. **(1) DECISION STATISTIC CORRECTED — D replaces F** (differential fragility now gates on **D(s)=ΔCLAP(0)−ΔCLAP(s)**; old F=D−E=(A0−As)−2(C0−Cs) is uninterpretable + can manufacture a false positive when E<0; algebra verified ~1e-17). Prereg **v4→v5** (`icassp_gate0_prereg.yaml` sha `231b98ed`; v4 preserved `e1c51244`); `cluster_bootstrap.severity_verdict` gates on D, F kept as deprecated provenance. **(2) PRIMARY endpoint = `p1_recovered`** (single → no MCC); `p1_pruned_ema_reconstructed` = secondary/mechanistic. **(3) Regression tests** `test_phenomenon_statistic.py` P1–P5 (D identity, paired-before-bootstrap, no-group-means guard ratio 0.01, E<0 false-positive) + `test_cluster_bootstrap.py` 8/8. **(4) Scorer PINNED** `laion/clap-htsat-fused` rev `365dea6e` (blobs safetensors `3f648de6`, bin `1ed5d021`); `gate0_rescore_control.py` re-scored the frozen 384 → reproduces the verdict EXACTLY (all diffs 0.0), historical verdict NOT rewritten. **(5) ONE parametric validator** `manifest_validator.py` (`test_manifest_validator.py` V1–V4; frozen 384 manifest validates 0 errors, 10 corruptions caught). **(6) SLICED ADAPTER built** `sliced_adapter.py`+`build_sliced_adapter.py`: 64/64 (24 positional (960→192), 40 identity), max restricted-ΔW err float64 = 0.0, p1 positional 64/64; **sha `5cc0a79a`** (dense ancestor `84a24a38`); `test_sliced_adapter.py` S1–S4. **(7)** same sliced bytes strict-load onto BOTH backbones (shape/name walk 64/64 identical; OFF no-op, ON changes forward deterministically — reconstructed 0.235 / recovered 0.020). **(8)** generator/scorer provenance hardened (git/env/GPU/source-ckpt/dense+sliced SHAs; OPENBLAS guard). **(9)+(10)** downstream 4 systems = {p1_pruned_ema_reconstructed, p1_recovered}×{off,on} = **768 NEW WAVs** (dense 384 reused byte-for-byte, same frozen x_T); CPU dry-runs GREEN both backbones (manifests validate 0 errors); entry `run_phenomenon_gen.sh`; verdict driver `phenomenon_score_verdict.py` (unified scoring, D-gate, primary=recovered) self-test PASS. **Projected 768-gen cost 1.36–1.77 cr → central chain ≈4.22–4.63 / 5.0.** GPU command NOT executed. Awaiting Gabriel's authorization of the falsifier.
* **ICASSP PIVOT — DECISION-V4-09 ADOPTED (2026-08-26 03:02; `docs/master_plan_v4_amendment_icassp.md` rev4 = binding contract). Authorized NOW = §H ONLY: CPU wiring + frozen held-out 64-prompt manifest + CPU dry-runs + ONE ≤0.1-cr GPU smoke; Gate 0 behind STOP-0; nothing downstream.** Frozen at adoption (`configs/research/icassp_gate0_prereg.yaml`): CI = cluster percentile bootstrap, two-sided 95%, B=10000, seed 20260826, **unit=prompt**; battery = **exactly 64 prompts × 3 seeds**; SESOI +0.025; Gate-0 recipe r8/α16/lr1e-5/batch2/200ep; smoke measures execution/cost ONLY (no tuning of SESOI/batch/epochs/rank/α/LR/filter/scorer). **Built + green (CPU, 0 cr):** statistical core `research_pruning/eval/cluster_bootstrap.py` + `tests/research/test_cluster_bootstrap.py` **8/8** (Gate-0 verdict, dual severity gate E/F, prompt-clustering demonstrably wider than the n=192 misuse, prereg-YAML↔code consistency). **RESOLVED THIS TURN (0 cr):** (a) **Zenodo correction** — Arshdeep's recovered **(1,2,3,1)** checkpoint is PUBLIC (record 21977996, `l1_p1_finetuned_global_step_999999.ckpt`, md5 `cfb7ca3f…`, 4.45 GB, CC-BY-4.0; recovered (1,2,1,1) too; no recovered p2_dp2); Arshdeep email CANCELLED; provenance corrected (OPEN ITEM #1, HANDOFF §9.3, memo §5, amendment §D). Recovered-backbone baseline UPGRADED from an ask to an in-scope published system (ledger ARSHDEEP-RECOVERED-PUBLIC). (b) **Stack mapped** (ledger ICASSP-WIRING-PLAN): reuse `audioldm_peft` (Kim recipe = `include_name_substrings=("to_q","to_v")`, target_conv2d off, bias/GN off) + `measure_tgen.build_model` (DENSE M-Full) + `generate_sample` + `.venv-metrics` CLAP; 4 new wrappers (dense+real-data trainer, generator, fused-CLAP scorer, smoke) + 3 fetches (Kim 193-clip set, MusicCaps CSV, `laion/clap-htsat-fused` — none on disk; AudioCaps local for dry-runs). **KIM-CLIP-LENGTH — AUDITED 2026-08-26 (ledger KIM-CLIP-LENGTH-AUDIT; `scripts/research/kim_clip_length_audit.py`, CPU 0 cr): the "256-fixed → must pad to 10.24 s" premise is REFUTED.** `latent_t_size` is a config var, not a weight-bearing param (U-Net strict-loads 690/690 at any length); VAE downsamples time ×4; the ONLY real constraint is **latent_t divisible by 8** (U-Net skip-connection symmetry) — so exact 4.0 s (latent 100) CRASHES, but **true near-4-s training is feasible at latent_t=96 = 3.84 s** (pure crop of Kim's 4 s, zero padding; loss well-defined; pruning materializer + sliced-LoRA mapping are time-independent). **VERDICT A (3.84 s) ADOPTED by Gabriel 2026-08-26 13:48 = DECISION-V4-10** (pre-data protocol correction). Prereg VERSIONED to v2 (`configs/research/icassp_gate0_prereg.yaml`: train_clip_seconds 3.84, latent_t_size 96, mel_target_length 384, **crop_policy=center** frozen; gen also 3.84 s); v1 preserved at `…_v1_superseded.yaml`. Kim released no crop convention → center crop is the default, frozen. Paper-facing: near-4-s M-Full transposition, NOT exact S-Full replication. Trainer to be built around 3.84 s / latent_t=96. **NEXT (still §H, 0 GPU until dry-runs green + STOP-0):** build the 4 wrappers, fetch the 3 deps, freeze the 64-prompt held-out manifest, CPU dry-runs, THEN one ≤0.1-cr smoke. Optimization target = defensible ICASSP-2027 paper by **2026-09-16** (**balance MEASURED 2026-08-26 = 5.0 account cr** via lightning_sdk, `docs/compute_budget.md`; supersedes stale ~9.6; **effective cap = min(3.5, 5.0−2.0) = 3.0 cr** binding, DECISION-V4-10 arithmetic correction; ≥2 cr reserve binding). §H: MusicCaps CSV + Kim-193 parquet FETCHED (gitignored); cluster-bootstrap 8/8; Zenodo recovered-(1,2,3,1) re-confirmed (not downloaded). **64-prompt held-out battery FROZEN — v2 LEAKAGE-FIXED** (`configs/research/icassp_gate0_battery.json`, prompts_sha256 **`ba4ebb50…`**, `--check` reproduces; v1 `46ee4203` preserved at `…_v1_superseded.json`; ledger GATE0-BATTERY-V2-LEAKAGE-FIX). **Source-leakage audit** (`gate0_leakage_audit.py`): Kim captions = MusicCaps captions + deterministic subgenre suffix → base recovered **45/45 exact to 44 source ytids** (`kim193_source_ytids.json`); **v1 had leaked 1 ytid (ZnBvXFDWpWo)** past Jaccard. v2 excludes PRIMARY by source ytid (disjoint asserted, 0 leak) + caption dup secondary. **Construction:** 193 clips = 193 distinct 4-s excerpts from only **44 source recordings** (~44 songs, not 193 independent). Kim data read via isolated `uv --no-project pyarrow` (frozen `.venv` untouched). **CPU PACKAGE COMPLETE (V4-12) — ALL DRY-RUNS GREEN (0 cr), awaiting ONE human review before the ≤0.1-cr smoke.** **V4-12 EMA weight convention (PRE-DATA, prereg v4):** upstream inference uses EMA (`use_ema=True` default; raw differs materially — dense mean-rel 2.4%, U-Net output max|Δ|≈5.9); runtime `ema_scope` invalid post-PEFT. Convention = materialize EMA into the U-Net BEFORE LoRA, disable runtime EMA (`research_pruning/eval/ema_weights.py`); **p1_pruned's stored EMA is BROKEN (238 tensors dense-shaped) → prune the DENSE EMA**; recovered uses its own EMA. Audit+demo PASS; trainer materializes EMA (dry-run PASS). **Production generator** `gate0_generator.py` (replaces e2e wiring): EMA per backbone, **guidance 2.5, DDIM 50, eta 0.0**, deterministic **paired noise** x_T=f(ytid,replicate) identical across all 6 systems (common-random-number; `--verify-paired-noise` PASS), full provenance rows; dense CPU dry-run PASS. **Adapter round-trip PASS** (save→fresh→load, max|Δ|=0 on weights/delta/forward). **Smoke wrapper** `gate0_smoke.py` = bounded executions of the PRODUCTION trainer+generator (reports VRAM/sec-step/sec-clip + cost projections; FP32; STOP-0 check) — **CPU dry-run PASS (wiring only, exit 0): reads recipe ddim50/guid2.5/eta0/fp32/ema, times train+gen, computes projections+STOP-0.** CPU sec/step≈8.6s (meaningless — GPU is the real measure; output self-labels "NOT a measurement"). **V4-11 recipe-fidelity (from Kim's code, PRE-DATA, prereg v3):** weight_decay 1e-5 (was PyTorch 0.01=1000×), grad-clip 1.0, GAUSSIAN LoRA init (audioldm_peft, backward-compat tests pass), train CFG-dropout 0, FP32, gen guidance 2.5 (Diffusers 0.32.2 default). **Production trainer** (`gate0_trainer.py` rewritten): ONE per-step path, `for epoch: for batch` → asserts global_step==19,400 + saves adapter-only weights+meta; scheduler==Diffusers-polynomial proven at 0/1/19400/97000; dry-run PASS. **Recovered p1 CPU-VALIDATED** (Zenodo 21977996, md5 ✓, U-Net strict-load 690/690, differs 200/200 from pruned-only=genuinely finetuned, forwards@96). **Falsifier revised:** dense/p1-pruned/p1-recovered (drop mild (1,2,3,4) to full curve; same 6 systems). **Paper skeleton** started (`docs/paper/icassp2027_scenario_b_skeleton.md`, result-independent sections). **Anti-sunk-cost fallback frozen** (amendment §I: STOP-1/2 → preserve negative for arXiv/workshop, no forced pivot). (1) Battery **v2 leakage-fixed** (`ba4ebb50…`; excludes by 44 recovered Kim source ytids; v1 leaked `ZnBvXFDWpWo`; ledger GATE0-BATTERY-V2-LEAKAGE-FIX). (2) fused-CLAP scorer dry-run PASS (`gate0_clap_scorer.py`). (3) Kim data @3.84s/center-crop (`preprocess_kim_clips.py`; all clips exactly 4.0s). (4) **Trainer** `gate0_trainer.py` dry-run **ALL assertions PASS** (LoRA only to_q/to_v 32+32=64, trainable 675,840=LoRA-only, base frozen, waveform 61440, mel 384, latent 96, U-Net==noise, **19,400 updates/200ep** from Kim's code drop_last=False). (5) **e2e wiring PASS** (`gate0_e2e_smoke.py`: generator→fused-CLAP→ΔCLAP→bootstrap→verdict; ledger GATE0-E2E-WIRING). (6) cluster-bootstrap 8/8. Kim training recipe inspected from `github.com/2025-comprehensive-design/AudioLDM-with-LoRA` (polynomial LR over 97000-step horizon, warmup 0). **Corrections committed. NEXT = Gabriel's review of this package, THEN the single ≤0.1-cr smoke (measures real Kim path, batch2, r8/α16, 3.84s, sec/step+VRAM+gen-timing), then STOP+report before Gate 0. Smoke NOT auto-run.** Thesis = **Scenario B**: legacy dense-trained LoRA → pruned backbone via **deterministic mask-induced slicing** (zero retraining, zero adapter data); phenomenon before method. Audits COMPLETE (`docs/publication_decision_memo.md`, §5 rev4): (1) **transfer proven executable at every seam** — `test_lora_mask_transfer.py` 6/6 + `test_scenario_b_seam_transfer.py` S1–S6 (238/238 changed tensors mapped, zero learned params; published p1 ckpt = pure selection, **pre-recovery/never finetuned**); (2) **Gate-0 substrate**: Kim et al. Appl. Sci. 2025 — recipe+193-clip data public, **weights NOT released**, published eval = 1 prompt × 5 seeds (noise-compatible) ⇒ Gate 0 = OUR powered replication on dense M-Full (r8/α16, 200 ep = first published/affordable operating point, **held-out** MusicCaps prompt battery disjoint from the 193 captions, **prompt-level cluster bootstrap**, SESOI +0.025, PASS=point≥SESOI AND lower-CI95>0, ≤1 cr, smoke-anchored, batch=2 locked); (3) **novelty: NO direct collision** (CA-LoRA/ASSP/LoRA-X/SVDQuant/X-Adapter contrasted; wording "we found no prior work…"; AudioLDM ecosystem n=1 → prospective, MusicGen ~196). **rev4 phenomenon gate (dual):** a severity qualifies iff **(i) standalone non-inferiority upper-CI95[E(s)]≤0.025 AND (ii) F(s)=D(s)−E(s) with point≥0.025 AND lower-CI95>0** (E=standalone CLAP loss, D=adapter-uplift loss); F positive but (i) failing = descriptive only. Claim scope = **standalone text-audio semantic alignment (CLAP)**, FAD/KAD corroborative. **Falsifier {dense,(1,2,3,4),(1,2,3,1)}×{±sliced LoRA} ≤2.5 cr is separate from a re-costed completion tranche** (adapter B independent replicate + full 5-pt ladder, **prioritized over the Scenario-A oracle**). **Zero-cost priority:** draft request to Arshdeep for the **recovered** checkpoint ready (`docs/arshdeep_recovered_checkpoint_request.md`) — if recovery restores standalone but not legacy-adapter utility, much stronger story; time-boxed, else paper states pre-recovery limitation. STOPs 0–3 pre-registered (fail ⇒ thesis dies, no iteration). **SEVERITY SWEEP (13–26 cr): proposed DEFERRED / NOT part of ICASSP** (below).
* **TIER-0 GPU CAMPAIGN ✅ COMPLETE (authorized 2026-08-20 16:00): Job 1 (D1+D2), Job 2 (Gate B′), Job 3 (screening) all DONE — each with CPU dry-run + GPU smoke, T4, clean pushed commit. Final spend 6.9150 cr (SDK, 20 jobs); 2.0-cr reserve INTACT (headroom ≈ 0.69). No Tier 1 without new auth (DECISION-V4-02).** **Job 3 (ledger TIER0-SCREEN-RUN, `screen-1` 1.412 cr + smoke 0.109):** heterogeneity screen on 200 prompts × {base, P0-std, P1-nat} × S=50 (600 clips), PANNs top-10 recall per event. **Event-level heterogeneity present** (P0-std between-event std_L 0.148 > mean 0.096; Rain/Engine/Siren forget, Speech/Sheep don't), **pattern moderately criterion-stable** (Spearman L_P0std–L_P1nat = 0.663 ≥ 0.5, p=0.073, n=8), **data-aware P1 protects better on the 8-event subset** (mean_L 0.048 < 0.096; **CORRECTED 21:00: pooled over all 200 prompts P1-nat 0.328 ≈ P0-std 0.336, no protection** — ledger SWEEP-PROPOSAL) → weak support for the RQ1′ direction, underpowered (only 8 events ≥5 prompts — Q7 predicted); latent between-event SD after sampling correction ≈ 0.12 (35 events) / 0 (8 events). Screening only; RQ1′ stays `unresolved`; Gate E (Tier 1) is confirmatory. **Job 2 (ledger TIER0-GATEB-RUN, `gateb-1` 0.4573 cr + smoke 0.1043):** per-slot P1 saliency on the enriched 512-ex pool (5120 slots) stored; **Gate B′ null half-half overlap median 0.9479, 5th-pctile 0.9401 ⇒ threshold 0.9401** (verdict needs a Tier-1 intervention variant). **Job 1 (ledger TIER0-D1D2-RUN, `d1d2-1` 0.3618 cr + smoke 0.1029):** D1 signed asymmetry small & consistently negative (text slightly more damaged: P0-std −0.145 ≈2% of norm; R_mod≈0.233); Gate-A screen (P0-std vs RAND×5) CI [−0.019,+0.009] ⊃ 0 → **reinforces the v3 RQ-swap negative** at the new primary P0-std. D2 guidance ΔG≈G (rel≈1.1). **⚠ FINDING-P0-COLLAPSE:** M3B artifact's `P0_L1`==`P0_published` (sum-norm cancels ±L1 sign) → M4-SCREEN-FOUND's P0_published-vs-P0_L1 gap was seed noise; fixed (P0-pub=published pkl, P0-std=reversed pkl, 0/2304 overlap). **Same fix needed in Job 3 (screening).**
* **CPU QUEUE (V4 Tier-0 prep, credit-free) — ✅ COMPLETE Q1–Q7 (`docs/NEXT_SESSION.md` §3).** **Q7 (ledger Q7/GATE-E-POWER):** `scripts/research/gate_e_power_sim.py` — parametric bootstrap of Gate E's exact rank test; FP rate at δ=0 = 0.049 ≈ α (validity); **frozen sentinel design 20×15×K_rand=20 has MDE δ=0.35 for 80% power at placeholder rates (p_base=0.6, μ_loss=0.15) — underpowered for small effects; concrete MDE/resize fixed once Tier-0 supplies real rates (M4-1b)**. Resize levers: 30 prompts→δ=0.20, 40 events→δ=0.25. New `tests/research/test_gate_e_power.py` E1–E5 PASS. **Q6 (ledger Q6/GUIDANCE-D1):** `m3a_diagnostics.py` gained template (`"a sound of [alias]"`) + counterfactual (`c_without_e`/`c_only_e`) text conditioning, `guidance_norm`/`guidance_probe` (uses Q2 strict map), and `d1_signed_per_stratum` (signed `‖E_a‖−‖E_t‖` + per-stratum, reported as `result["D1"]`); `--guidance-probe` flag. **CPU dry-run PASS** (guidance probe on real base model: `G_tmpl,F=3.77`, `G_event,F=1.61`, deleted a real event span; D1 per-stratum present). **Q5 (ledger Q5/GATE-B-PRIME):** Gate B′ machinery — new `research_pruning/paired_modality/gate_b_prime.py` (`per_slot_saliency`/`recompose_mean`/`null_split_overlaps`/`gate_b_prime`), `m3b_saliency.py --per-slot-store` hook (validated in dry-run: 28 layers × slots, re-loads + drives null-split on CPU). New `tests/research/test_gate_b_prime.py` G1–G4 PASS: recompose==accumulate (FP), null median 0.917≫chance, fires on independent saliency, no-fire on identical. **Q4 (ledger Q4/MATERIALIZE-PARAM):** `random_masks.py` `build_pruned_unet`/`materialize`/`kept_counts`/`ranking_driven_layers`/`build_random_null` parameterized by `channel_mult` (default `(1,2,3,1)`, backward-compat); `count_pruned_params` added. New `tests/research/test_materialize_channel_mult.py` P1–P4 PASS: params 145,673,864 exact; `(1,2,3,4)`=317,308,040 exact; mild-budget materialize strict-loads+forwards; **R5 690/690 bit-exact preserved via explicit `channel_mult=[1,2,3,1]`**. Existing `test_random_masks.py` R1–R5 still PASS. **Q3 (ledger Q3/FINELAP-SMOKE) resolves DECISION-V4-06 = PASS → H-acoustic STAYS in primary Gate M:** `AndreasXi/FineLAP` (MIT, weights sha256 `13b9646c…`, gitignored) loads+runs on CPU under torch 1.13.1 via two documented env-gap adapters (nested-config shim + local-package load; no pin/code change); `get_frame_level_score→(5,10,64)`; own-event beats distractors 5/5 by 1-2 orders; frozen occupancy rule `mean_t[score≥0.5]×10.24s` separates brief (Gunshot 0.96s) from sustained (Applause 10.24s). Script `scripts/research/finelap_smoke.py`. **Q2 (ledger Q2/MANIFEST-FREEZE):** plan-v4 pre-registration manifests frozen + sha256 in ledger, built deterministically by `scripts/research/build_v4_manifests.py` (`--check` bit-identical); strict requested-event map reproduces REVIEW-004 counts exactly (Siren 858/897, Gunshot 415/836, single-label 9637). **E\*=101(T0)/98(T1); calibration 256 natural+256 tail=512; mechanism 50×20=999; holdout 500 (blinded); sentinel 20×15; screen 200 — all pairwise-disjoint at wav id and disjoint from val (0 overlap).** Files in `configs/research/` (+ fetched `audioset_ontology.json`). **Q1 (ledger Q1/M4-SCREEN-RESCORE):** FAD/FD NaN fixed and all 6 screening systems re-scored from cache (no re-gen) — **base FAD 0.945 / FD 73.8 best; pruned 1.31–1.62 / 81–87; P3 worst** (screening-only, 1 seed, `claims_matrix` unchanged). IS reproduces M4-SCREEN-FOUND to 5 dp (same audio). **En route found E-BLAS (ledger E-BLAS): numpy 1.23.5's bundled OpenBLAS 0.3.20 silently returns WRONG matmul/eigh/svd/cov on this Xeon 8488C (n>64) — FIXED by `OPENBLAS_CORETYPE=Haswell` (`scripts/research/install_blas_fix.sh` `.pth` + `research_pruning/eval/_blas.py` raise-guard); re-run the script after any `.venv` rebuild.** Follow-up: spot-check prior CPU numpy linalg (M3A OLS expected safe). New: `research_pruning/eval/frechet.py`, `tests/research/test_frechet.py` (T1–T6 PASS).
* **PLAN V4 ADOPTED (2026-08-20 14:13, ledger DECISION-V4-00..07). Contract = `docs/master_plan_v4.md`; v3 superseded (kept for history).** Gabriel adopted all eight recommended defaults verbatim: P0-standard primary baseline / P0-published reproducibility control (V4-01), **Tier 0 only authorized (~2.7 cr; balance ~3–5 cr spendable; 2.0-cr reserve stands)** (V4-02), ELSA deferred (V4-03), event-set/null frozen `N_min=200`/mild `(1,2,3,4)`/`K_rand=20` (V4-04), RQ-swap kept as one-paragraph negative (V4-05), FineLAP conditional on CPU smoke (V4-06), partition/sentinel/MDE/Gate-I margins set (V4-07). Propagated to v4/v3/`AGENTS.md` step 2/`claims_matrix.md`/`HANDOFF.md`. **Resume point `docs/NEXT_SESSION.md`; now executing the credit-free CPU queue Q1–Q7 (§3); no GPU job without Gabriel's explicit go.**
* **PLAN V4 rc4 = ADOPTED CONTRACT (2026-08-20, ledger REVIEW-002..005 + DECISION-V4-00; `docs/master_plan_v4.md`).** Reframing agreed by both reviewers: event-level semantic forgetting under TTA pruning → mechanism (exposure / guidance / acoustics) → fixed-compute calibration intervention → PEFT as cost baseline. AudioCaps exposure gradient reproduced in-repo (Speech 41.5 % … Explosion 0.47 %, Music 0 %). **Credits: 4.205 spent on 14 jobs; ≈3–5 spendable.** Tier 0 (≈2.7 cr) fits; Tiers 1–2 need funding. rc2 fixes two rc1 errors (Gate E not executable in Tier 0; RAND×2 is not a null → `K_rand ≥ 10`), measured mild budget `(1,2,3,4)` = −23.7 %. **Both reviewers vote adopt.** rc3 adds the identification layer (event-specific counterfactual H-guidance, event-specific acoustics, `K_rand=20` + power simulation, P1-placebo, disjoint mechanism/holdout sets, Gate I non-inferiority margins, audio- vs caption-exposure split); Tier 1 ≈45 cr, Tier 0 ≈2.7 cr (screening only). rc4 = pre-registration consistency pass (`K_rand=20` exact rank test, Gate M on event-specific covariates, CV tie-break, matched placebo, mechanism-general intervention recipe, FineLAP confirmed public + fallback, seed pairing). **Both reviewers: YES to V4-00; rc4 proposed as frozen protocol.** Eight decisions DECISION-V4-00..07 pending from Gabriel.
* **HOSTILE REVIEW ROUND 1 (2026-08-20) — `docs/review/2026-08-20_reframing_round1.md`.** Conclusion: M3A/M3B are rigorously executed but the instruments are insensitive to the hypothesis by construction (modality is 1.5 % of the ε signal; conditioning enters only via `film_emb`), and the 2026 literature (COMET, DASH, OBS-Diff, MosaicDiff, Arshdeep arXiv 2607.13330) narrows the original gap. **No M5 and no further M4 until the contribution claim is redefined (plan v4).** Candidate reframing + literature questions for the second reviewer are in that file.
* **M3B-002 FINDING + DECISION (2026-08-19) — `docs/m0_baseline_reproduction/l1_pruning_direction_finding.md`.** The published PruningAudioLDM L1 checkpoint keeps the **lowest**-magnitude conv filters per pruned layer (inverted from standard L1), verified 4 ways (reference's own `np.argsort` ascending + bit-exact materializer keeps `[:k]`; 15/15 pruned layers; Spearman -1 vs P0 on all 28 layers) and re-derived independently (AUDIT-NIGHT2). **A property of Arshdeep's official artifact itself. DECIDED (Gabriel, DECISION-M3B-002):** because RQ2's L1 baseline IS Arshdeep's published pruning artifact (Zenodo 21376822), the project's P0 **adopts the published inverted convention: keep LOWEST-L1** (`p0_importance(convention='published')`, default `P0_CONVENTION='published'`). Verified to reproduce the published kept-set **exactly 12/12 ranking-driven layers** (`scripts/research/verify_p0_convention.py`; control test C8). `'standard'` L1 retained for non-Arshdeep baselines only. RQ3's inverted-starting-point caveat stands. Reproduce the finding: `scripts/research/verify_l1_direction.py`.
* **GATE B AMENDED + Gate B statistic IMPLEMENTED (DECISION-M3B-003 / M3B-003, 2026-08-19), CPU, NO scientific result.** Resolves audit findings **G1** (the master plan's Gate B was *mathematically impossible* as written) and **G2** (the draft protocol carried two contradictory overlap definitions) — the last non-GPU blocker for freezing `docs/pilot_protocol.md`. **Decision 1 (Gabriel, option (a)):** the single overlap definition is the **KEPT set** and the plan's numerals transfer verbatim onto it — Gate B PASS = weighted kept-set overlap `<= 0.80` AND `>= 2` ranking-driven layers `<= 0.70`; the prune-set number is reported only via the exact identity `(N-2k+|K_a∩K_t|)/p`. Verified against the real artifact (`scripts/research/verify_gate_b_geometry.py`, exit 0): **12 ranking-driven layers of 28, every one N=960 / k=192 / p=768**, kept-set chance **0.2000**, prune-set pigeonhole floor **0.7500** on all 12 → the plan's 0.70 prune-set threshold is confirmed unreachable; the amended thresholds sit strictly between chance and identity. **Decision 2 (Gabriel):** since P0 keeps LOWEST-L1 but P1/P2/P3 keep HIGHEST saliency, `p0_importance('standard')` is also reported as a **secondary reference** (zero extra compute) and every comparison must be worded "vs the published L1 pruning artifact", never "vs standard L1". **Code:** `research_pruning/paired_modality/overlap.py` (`kept_set_overlap`, `weighted_overlap`, `evaluate_gate_b`, `GATE_B_WEIGHTED_MAX/LAYER_MAX/MIN_LAYERS`); weighted aggregate evaluated as `Σ|K_a∩K_t| / Σk` so boundary comparisons are FP-exact. Tests `tests/research/test_overlap_gate_b.py` **O1–O6 PASS** (identity→FAIL, independence→chance 0.2170/PASS, prune-floor identity vs brute force, both gate conditions necessary, inclusive/exact boundary, 9 guards + seam-dilution proof). **DECISION-M3B-002 also propagated into `docs/pilot_protocol.md`, where it was missing.** Full research suite **12/12 modules PASS**. Nothing evaluated on real saliencies; the protocol is still UNFROZEN (needs GPU `T_sal`/`T_fwd` + Compute Gate CG). `git diff upstream-frozen -- audioldm_train/` still empty.
* Repository: `/teamspace/studios/this_studio/audioldm-modality-swap-pruning`, branch `main`, remote `origin` = `gbibbo/audioldm-modality-swap-pruning` (public). No prior research repository was recoverable (`gh` or filesystem) — history started fresh; the M0/M1 work the master plan describes as done was local-only on the author's Windows machine and unpushed.
* **Frozen references imported and verified in-repo.** `upstream-frozen` = `702a638d…` (full 35-commit upstream history merged into `main`); `pruning-reference-frozen` = `6f65f628…` (kept as reference branch, plus a working clone in `_external/`). `git diff upstream-frozen -- audioldm_train/` is **empty**.
* **`(1,2,3,1)` verified from artifacts, not from documents.** Base `channel_mult=[1,2,3,5]`, pruned `l1_audioldm-m-full_p1.ckpt` `channel_mult=[1,2,3,1]`, `model_channels=192`. U-Net 415.955 M -> 145.674 M params (-65.0%).
* **Full-FT checkpoint gate RESOLVED (deadline was today).** All 2061 same-shape tensors of the published pruned checkpoint are bit-identical to the base: it is pure prune-and-merge, **never finetuned**. The L1 `(1,2,3,1)` pre-recovery checkpoint is public and fetched; the **recovered full-FT `(1,2,3,1)` checkpoint is proven NOT public**. RQ3 is downgraded to a published-reference comparison until Arshdeep supplies it.
* **All public artifacts fetched, md5-verified and extracted** (`fetch rc=0`): base/pruned/U-Net checkpoints, `sorted_indexes_dict.pkl`, `checkpoints.tar` (7 aux checkpoints: CLAP, AudioMAE, VAE, HiFi-GAN 16k/48k) and `dataset.tar` (AudioCaps, 31 GB, 50 961 wav). Manifest: `docs/m0_baseline_reproduction/dataset_manifest.md`.
* **Upstream `tests/validate_dataset_checkpoint.py` PASSES**: structure complete and all 50 466 referenced audio files (49 502 train + 964 test) resolve on disk. Dataset load smoke test passes on both splits with the expected shapes (10.24 s @ 16 kHz, 64 mel bins).
* **Known trap:** `dataset_root.json` maps the **val split to the same file as test** (964 items). A disjoint validation split must be defined explicitly before any model-selection decision, or test contamination is structurally possible.
* **Reproducible environment BUILT and verified.** `.venv` on a `uv`-provisioned standalone CPython **3.10.20**, installed from the unmodified frozen `poetry.lock`: 155 packages, 0 errors, no pin relaxed (`torch 1.13.1+cu117`, `transformers 4.30.2`, `pytorch-lightning 2.1.1`, `librosa 0.9.2`, `numpy 1.23.5`, plus `audioldm_eval` and `hear21passt`). Lightning blocks `conda create` in every form and `/commands/python3.10` is a shim that runs 3.12; both are documented in `docs/environment_report.md`.
* **Model-loading smoke test PASSES.** `UNetModel` rebuilt from the frozen config and loaded `strict=True` for both budgets: `[1,2,3,5]` 415.955 M and `[1,2,3,1]` 145.674 M, 690 tensors each, 0 missing / 0 unexpected. Import smoke tests 6/6 including `audioldm_eval`. The M0 criterion "base and pruned architectures can be reconstructed deterministically" is **met**.
* **M2 conditioning-path validation COMPLETE (PASS), 100% CPU.** `research_pruning/diagnostics/conditioning.py` exposes the audio/text CLAP paths and FiLM epsilon prediction, faithful to `LatentDiffusion.apply_model` (proven by file:line in `docs/condition_swap_validation.md`). Tests `tests/research/test_conditioning_paths.py` **T1..T5 all PASS**: e_a/e_t are `[B,1,512]` into the same FiLM interface (`extra_film_condition_dim==512`, `film_emb 512→768`); determinism `max|Δ|==0.0`; pairing proven by tensor hash; non-degeneration `mean|eps_a−eps_t|=1.148e-2`. CLAP embeddings are **L2-normalized for both modalities** (‖e‖₂≈1.0, std ~4e-8, N=48); paired cosine 0.248 vs cross −0.079 (sanity PASS). **Two traps recorded:** (i) CLAP truncates the audio branch to 10.0 s (drops ~0.24 s of every 10.24 s clip, `data.py:452,460`); (ii) upstream `unconditional_prob=0.1` is **not** overridden by the config — the diagnostic forces 0.0, and M3 calibration must too. Report `docs/condition_swap_validation.md`; artifacts in `artifacts/m2_condition_swap/`. `git diff upstream-frozen -- audioldm_train/` still empty.
* **RQ1 DIAGNOSTICS RESULT — Gate A FAILS (M3A-DIAG-RUN, 2026-08-20 04:30, job `m3a-diag-1`, T4, 0.8414 cr).** Diagnostics `D_gen/D_mod/R_mod` (`modality_diagnostics.py`) + matched random null (20 pre-registered masks, seeds 20260818..37) + Gate-A statistic (`matched_null.py`, linear OLS `R_mod~D_gen`, Delta_swap, bootstrap unit=wav) run on the real base vs L1-pruned model over the disjoint val split (W=200/K=5/1000 slots). **L1 `R_mod=0.2298` vs random `0.2279`; matched on `D_gen` (8.171 vs 8.096, interpolated), `Delta_swap=0.0007`, 95% bootstrap CI [−0.0025,+0.0028] (includes 0), std residual 0.459 < 0.50 → FAIL.** Structured pruning does NOT cause modality-dependent damage beyond a matched random null — **RQ1 rejected**. Audited (L1 interpolated in the random cloud; `R_mod` non-degenerate). **Reinforces RQ2**: audio/text saliencies agree (ρ=0.98), so pruning damages both alike. Materializer bit-exact 690/690 (R5); machinery tests D1-D5/R1-R5/N1-N4 PASS. Result: `artifacts/m3_pilot/m3a_result.json` (gitignored).
* **Disjoint validation split DEFINED (M0 9.1 resolved).** `configs/research/val_split_disjoint.json` = upstream AudioCaps val (495 items), proven disjoint by wav id from test (0) and train (0); sha256 `e540146d…`. `dataset_root.json` NOT modified. **Note:** `vae_mel_16k_64bins.ckpt` is NOT the AudioLDM-M-Full VAE (204/398 tensors differ) — use the embedded first_stage; recorded in `dataset_manifest.md`.
* **GENERATION STACK WIRED + M3B SALIENCY RUNNER BUILT & AUDITED (2026-08-20 ~01:20, CPU-validated).** Two new GPU-ready scripts, both with `--dry-run-cpu` + fail-fast preflight (commit/tree/ckpt/CUDA/manifest-sha), both CPU-dry-run validated end-to-end: (1) `scripts/research/measure_tgen.py` builds the full `LatentDiffusion` and times real `generate_sample` (DDIM+decode+vocoder) to MEASURE `Tgen` at S=50/200 — U-Net loads 0-missing; two CPU-only shims (`torch.load` map_location, `DDIMSampler` device) active only under `--dry-run-cpu`. (2) `scripts/research/m3b_saliency.py` = **the RQ2 saliency run**: P0-published/P0-L1/P1/P2/P3 + Gate B on the base `(1,2,3,5)` U-Net over the 28 L1 layers, consuming the frozen calibration manifest, **batch=1 per slot** (faithful to `mean_slots|g·∂L/∂g|`). **`/auditar` (ledger AUDIT-M3B-SAL) CONFIRMED the machinery correct by re-execution AND found+fixed one INVALIDATING bug:** a custom `dataset_json` bypassed path resolution → the audio branch was fed **EMPTY (silent) waveforms**, which would have made `S_a`/P2/P3 worthless with no crash. Fixed (resolve wavs to `metadata_root["audiocaps"]` + assert-exists); verified real nonzero audio and `S_a≠S_t`. Est. full run E=256/K=5: ~1.7 GPU-h ≈ 1.5 cr.
* **RQ2 SALIENCY RESULT — Gate B FAILS (M3B-SALIENCY-RUN, 2026-08-20 02:55, job `m3b-saliency-2`, T4, 0.4266 cr).** First RQ2 result on the real base model (E=256/K=5, budget matched 2560/criterion, frozen manifest verified). **Weighted kept-set overlap 0.9475 (threshold ≤0.80), 0 layers ≤0.70 → FAIL.** Audio and text Taylor saliencies agree almost completely (mean per-layer Spearman **0.980**, uniform 0.927-0.979 across all 12 ranking-driven layers), so the paired criteria collapse onto text-only: **P1 vs P2 = 0.948, P2 vs P3 = 0.987**. **`/auditar` says REAL, not artefact:** S_a≠S_t bitwise (0/28 identical); discriminating control **Spearman(S_a, P0-L1) = 0.571 ≪ 0.980**, so the tool separates criterion *type* but finds the *modality* effect small; FiLM reaches all gated ResBlocks; embeddings genuinely differ (M2 cosine 0.248). **Negative result for R2 (joint pruning)** — the modality-swap gives no pruning-relevant saliency divergence. Result: `artifacts/m3_pilot/m3b_saliency_result.json` + `.pt` (gitignored). Five pipeline scripts built+validated (`measure_tgen`✓measured, `m3b_saliency`✓, `m4_screening`✓, `m4_eval`✓CPU-free, `m5_recovery_probe`✓deferred); Job C materialization de-risked (saliency→ranking→`materialize`→145.674M strict-load).
* **`Tgen` MEASURED (M0-006-TGEN, job `tgen-2`, T4): S=50 → 8.44 s/clip, S=200 → 32.34 s/clip, peak 8.29 GB. FINDING: the DERIVED Tgen was ~2.4× LOW** — classifier-free guidance runs 2 U-Net forwards/step, which the ×1 derivation ignored. Corrected in `docs/compute_budget.md`. Consequence: S=50 screening (6 models × 200 clips) ≈ 2.81 GPU-h ≈ 2.5 cr (not ~1).
* **GPU JOBS + SPEND (2026-08-20):** `tgen-1` (interruptible) PREEMPTED mid-run → 0.068 cr (**lesson: interruptible only for LONG resumable jobs; short/non-resumable go on-demand**); `sal-smoke-1` FAILED on **dirty-tree preflight** → 0.114 cr (**lesson: the Job snapshots the tree at PROVISION time, so do not edit the repo after `lightning job run` until the job is Running**); `tgen-2` Completed → 0.204 cr; `m3b-saliency-1` RUNNING. **Session GPU spend so far: 0.39 cr.** Budget plan: saliency ~1.4 + screening ~2.5 + (deferred) M5 probe ~1.0; hard reserve 2.0 (spendable ~7.6). M5 probe runs only if budget clears after screening.
* **`docs/pilot_protocol.md` FROZEN (M3B-FREEZE, 2026-08-20 00:53 MVD).** Both prerequisites met (measured `T_sal`/`T_fwd`; CG resolved). Calibration slots materialized into a hashable pre-registration `configs/research/calibration_manifest.json` (tracked) by `scripts/research/build_calibration_manifest.py` — **256 wav ids (seeded draw order) + first-caption + every per-slot timestep**, **sha256 `8d7de065…`**, deterministic across runs, per-stratum coverage exact (256 paired / 512 P1), all from train (disjoint from test/val). E=256 held (≈0.81 GPU-h ≈ 0.72 cr with measured `T_sal`; not shrunk post hoc). **The freeze gate is CLOSED — but P1 Taylor + real loss closures must pass `/auditar` before any saliency number is treated as a result** (freeze does not waive it). The saliency run MUST consume the manifest, not re-draw slots.
* **M1 PEFT scaffold ADOPTED + audited-defects fixed (M1-005), CPU dummy-model tests PASS.** `audioldm_peft/` is now a working package (was a skeleton): LoRA Linear/Conv2d (F5 factorised conv, merge/unmerge bit-exact), injector + order-safe `freeze_for_peft` + `assert_peft_ready` guard (F4), unconditional aux-trainable counting (F3), explicit `train_layernorm_affine` with separate `layernorm_affine` reporting (F2), full-resume `training_state_dict`/`load_training_state_dict` (F7), trainable-only EMA, optimizer groups. Tests `tests/research/{test_lora_layers,test_injector,test_state_ema_optimizer}.py` **L1–L3, J1–J4, S1–S3 all PASS** via stdlib runner `scripts/research/run_research_tests.py` (F1: pytest absent, no pin relaxed). **F6 DONE (M1-006):** `test_peft_real_unet.py` R6a/R6b/R6c PASS on the actual `(1,2,3,1)` U-Net — 284 modules wrapped (185L+99C), LoRA 3,718,784 / bias 108,680 / GroupNorm 48,768 / LayerNorm 0, trainable 3,876,232 of 149,392,648; merge/unmerge max|Δ| 1.0e-7. **F8 DONE (M1-007):** `audioldm_peft/integrate.py` (`setup_peft`, `build_peft_optimizer`, `build_trainable_only_ema`, `peft_config_from_yaml`) + `test_peft_integration.py` I1–I4 PASS (incl. post-load-order proof) — CPU hooks only, `audioldm_train/` NOT patched (diff empty). **M1 CPU ACCEPTANCE COMPLETE: full suite 17/17 checks across 5 modules; all 8 audit defects F1–F8 addressed.** Only M1 **GPU acceptance** (several hundred real steps, VRAM, sec/step, resume) remains — blocked on GPU + Compute Gate CG. `research_pruning/{taylor,paired_modality}/` remain skeletons. `git diff upstream-frozen -- audioldm_train/` still empty.
* **M0 eval pipelines EXERCISED end-to-end (M0-006), CPU, pipeline-smoke only.** FAD/KL (`audioldm_eval` 0.0.5) and PANNs top-10 both run on real AudioCaps clips; invocations + findings in `docs/m0_baseline_reproduction/eval_pipeline_closure.md`. PANNs top-10 fully works (20/20 clips, coherent labels). FAD/KL: resolved deps (PANNs Cnn14 16k `ckpt/Cnn14_16k_mAP=0.438.pth` from Zenodo, CPU-sanitised; VGGish via torch.hub) and 4 library findings — **F-eval-3: audioldm_eval's VGGish FAD is unusable as-is** (sqrtm imaginary > tol, and `eval.py` crashes on the error sentinel); worked around to NaN so KL/IS/FID run; a real eval must use standard real-part FAD. Values are non-scientific (arbitrary disjoint subsets). Scripts `scripts/research/{fad_kl_smoke,panns_topk}.py`; checkpoints/CSV/folders gitignored.
* **P0–P3 pruning-criteria MACHINERY COMPLETE (M3B-000), CPU, control-tested, NO scientific result.** `research_pruning/taylor/` and `research_pruning/paired_modality/` moved from skeletons to a working criteria package: channel-gate first-order Taylor saliency (`gates.py` `ChannelGate` g_c=1, `attach_gates`), `accumulate_taylor` (S_c=mean|g_c·∂L/∂g_c|), within-layer normalization (sum/max/l2), P0 L1 magnitude, P2 mean / P3 max combine, `assert_matched_budget` (§5: P1 2B == P2/P3 B+B), and `compute_criteria` orchestration sharing S_a/S_t. Tests `tests/research/test_taylor_saliency.py` **C1–C7 PASS** on control models. **Prunable layer set now VERIFIED on the real U-Net (M3B-001):** `research_pruning/taylor/layer_set.py` maps the 28 public L1 ranking keys to Conv2d modules of the base `(1,2,3,5)` U-Net (widths 384/576/960), all out_channels matching; `test_prunable_layer_set.py` V1/V2 PASS — gates attach with **bit-identical** output (max|Δ|=0.0). **Still NOT done (needs GPU/frozen protocol + `/auditar`):** the slot/timestep construction, real audio/text loss closures, and any actual saliency computation on the base model — no P0-P3 ranking has been computed on the real model. **P1 is scientifically load-bearing.** Full research suite **10/10 modules PASS**. `git diff upstream-frozen -- audioldm_train/` still empty.
* **JOB RULE (learned the expensive way):** launch a Lightning Job only from a **clean, committed, pushed** tree, and always `cd` into the repo in the command. `gpu-benchmark-1` died on a missing `cd` (0.107 credits) and `gpu-benchmark-2` was refused by its own dirty-tree preflight guard (0.091). Both failed fast by design; ≈0.198 credits total, now encoded as rules. Verified job topology: `/teamspace/jobs/<name>/snapshot` (what the job saw, readable from the Studio for free) and `/teamspace/jobs/<name>/artifacts` (what it wrote). Jobs DO see the checkpoints, the 30 GB dataset and `.git`.
* **FIRST REAL GPU MEASUREMENT (M0-005-RUN, 2026-08-19) — `docs/compute_budget.md` IS POPULATED.** Lightning Job `gpu-benchmark-3` on a **Tesla T4 (14.562 GB)**, Studio left on free CPU, status Completed, **cost 0.1372 credits**, measured at clean commit `e6f50f4`. Raw JSON `artifacts/m3_pilot/compute_budget_measured.json` (md5 `12f8fef8…`). **MEASURED (batch 8):** `TRAIN_SEC_PER_STEP` **1.672427** / peak **4.177 GB**; `SALIENCY_SEC_PER_GRAD_EVAL` **1.596534** / peak **4.152 GB**; `FORWARD_SEC_PER_DIAGNOSTIC_BATCH` **0.465546** / peak **1.540 GB**. `GEN_*` **NOT MEASURED** (generation stack unwired). **F9/F10 confirmed on CUDA**: the job's R7a gate passed on GPU — 284/284 adapters with non-zero grads, 0 frozen params with any. **Escalation 1/2/4/8 with NO OOM**; `MAX_STABLE_BATCH=8` is the largest **tested**, not the largest that fits — **10.385 GB were still free** and per-sample cost was still falling (0.3897 → 0.2051 s/sample). **My earlier worry that a 16 GB T4 would be tight was wrong** (peak 4.18 GB ≈ 29% of the card); extend the ladder to 16/32 before costing long runs.
* **COMPUTE GATE CG: RESOLVED (Gabriel, 2026-08-20, DECISION-CG-001) — resolved by descoping.** Authorized now: core RQ1+RQ2 (M1 GPU acceptance, M2, M3A, M3B, pruning, **screening generation at DDIM S=50** + FAD/KL/PANNs) plus a **~2k-step M5 convergence probe** (loss-vs-steps for the step-count lever). Deferred to a top-up: S=200 confirmatory generation, full M5 recovery. **Hard reserve 2.0 credits** (spendable ≈ 7.6 of ~9.6). Interruptible instances: trial with one cheap job first (`--interruptible` verified in the CLI), then use for long jobs. At screening, BOTH `P0-published` and `P0-L1` are materialized/generated (superset; primary/secondary labeling stays open per REVIEW-001; DECISION-M3B-003 wording constraint unchanged). **Consequence: the pilot-protocol freeze is UNBLOCKED and must land in a commit before any saliency result is inspected.**
* **OPERATING POLICY FROZEN (Gabriel, 2026-08-19).** All development, debugging, tests and preparation happen on the **free CPU Studio**; the GPU is used **only** via `lightning job run` (Jobs snapshot the full Studio environment, so the filesystem question is closed); **the interactive Studio is never switched to T4 again**; and any GPU failure reproducible on CPU returns immediately to CPU — the T4 is not a debugger. Pattern to follow: CPU leaves the payload fully validated -> one GPU Job answers only GPU-only questions -> any bug goes straight back to CPU.
* **M1-009: F10 FIXED via the project's FIRST upstream patch; the missing backward regression test added (2026-08-19), CPU, VERIFIED.** F10 reproduced on CPU exactly as claimed (16 `BasicTransformerBlock`, all `checkpoint=True`, forward OK, backward raises). **Root cause is broader than first recorded:** `attention.py:379` defaults `BasicTransformerBlock.checkpoint=True` **independently of the U-Net's `use_checkpoint` flag** (absent from the config, so `False`) — the rejected `use_checkpoint=False` option **would not have fixed it**. **Patch:** `CheckpointFunction.backward` now differentiates only w.r.t. params with `requires_grad`, re-expanding with `None` in the frozen slots to keep the return arity. `git diff upstream-frozen -- audioldm_train/` is now **1 file, 16+/2-** (8 of them comment) — a recorded deviation, no longer empty. **New tests `tests/research/test_peft_backward_real_unet.py` R7a–R7d PASS:** one real fwd+bwd+opt step on the real pruned U-Net with REAL weights gives **284/284 `lora_B` non-zero grads**, 0 `lora_A` (correct — `B` zero-init), **0 frozen base params with grad**, 568/568 LoRA tensors moved and 0 base tensors changed after `opt.step()`; checkpointed vs non-checkpointed gradients agree **max|Δ| = 0.000e+00** over 568 tensors. **FINDING (zero-init trap):** on a freshly-initialised U-Net `out.2` is `zero_module`-ed (`sum|W|=0`), so gradient reaches only **1 of 284** adapters — any backward test on a random-init model is vacuous; with real weights that tensor is `sum|W|=174.98` and all 284 get gradient. R7c asserts both halves. **Suite 13/13 PASS; R5 bit-exactness still 690/690** (the patch changes no numerical result). **M1 GPU acceptance is still NOT claimed** — no optimization step has run on a GPU.

## OPEN ITEMS

0. **SA3 LINE CLOSED as an active research line (Gabriel, 2026-08-26). No F2, no ecological adapters, no RQ3/CASE-C; the remaining 1.3092 cr is NOT authorized for SA3. Postmortem `docs/sa3/sa3_postmortem.md`; closure claims in `docs/claims_matrix.md` (SA3 section).** All three SA3 questions terminated: **SA3-RQ1 CLOSED** (I_PT redundant with D_P — no distinct adaptation-resource structure); **SA3-RQ2 CLOSED/FAILED** (single-block positive controls L_6/L_13 passed field + host-collapse + external but FAILED the task-level ΔT_AA gate — field/function gap; `control_verdict.json`); **SA3-RQ2b CLOSED/FAILED** at the preregistered F1 STOP. **F1 (job `sa3-f1-1`, T4, Completed, 0.3223 cr, commit ede6f99):** a non-zero full-backbone LoRA r16/α16 attached across all 20 blocks (`max|lora_B|=1.197`, sha256 `b6ad50c0…` — note `max|B|` alone is NOT a functional-magnitude measure), trained on `mechanical` 96 train / 1000 steps, scored with the frozen paired CLAP audio-audio T_AA on 64 held-out eval. **ΔT_AA(base)=+0.0118 CI[−0.020,+0.043]; ΔT_AA(post)=+0.0230 CI[−0.011,+0.058]** — ≈6.3× (base) and ≈3.3× (post) **smaller** than SESOI 0.075, and **both upper CI bounds fall below SESOI** (stronger than "fail to reject 0": the data are incompatible with a SESOI-magnitude effect on either checkpoint). Symmetric gate → `STOP_RQ2B_BASE_FAIL` (`f1_verdict.json`). **Final allowed claim:** *Under the frozen standard recipe (full-backbone LoRA r16/α16, 1000 steps, mechanical 96 train / 64 held-out eval), the adapter failed to produce a functionally relevant paired T_AA uplift on either dense base or dense post.* Do **NOT** generalize to "LoRA does not work on SA3" or "adapter-compatible pruning is false". This does remove the two L6/L13 ambiguities (single-block support; n=8 power). **NOT RUN (not TODO):** F2 band control, ecological adapters, RQ3/CASE-C, any A_eco/A_tan/D_P inspection against F1. **Reusable machinery preserved** (trainer `--backbone`, `stage_trainL`, `f1_task_gen`, `f1_verdict`, `f1_run`, `f1_accept`, T_AA scorer, decision core). **Future-revival principle:** any revival of "prune now, preserve unseen adapters later" must START from an independently-established adapter/benchmark with demonstrated dense functional uplift BEFORE any pruning analysis — do not construct another positive control inside SA3 to manufacture that prerequisite. Budget: SA3_GPU 3.6382, CPU 0.0526, **SA3_TOTAL 3.6908, headroom 1.3092 (frozen; not for SA3)**. GPU stopped.
1. **Recovered full-FT `(1,2,3,1)` checkpoint is NOW PUBLIC (CORRECTED 2026-08-26, ledger ARSHDEEP-RECOVERED-PUBLIC) — no request needed.** Arshdeep's README (updated 2026-08-17) points at Zenodo **21977996** ("Pruned and finetuned Models"), superseding 21376822 as a strict superset (old files keep identical md5s) + adding recovered **(1,2,3,1)** `l1_p1_finetuned_global_step_999999.ckpt` (md5 `cfb7ca3f…`, 4.45 GB, CC-BY-4.0) and recovered **(1,2,1,1)** `l1_p1_dp1_finetuned_...ckpt`. No recovered p2_dp2. **Caveat:** `_finetuned_global_step_999999` files may carry optimizer/EMA state (larger than pruned-only) — verify weights-only on download. Not yet fetched (needed at the phenomenon stage, not Gate 0). Arshdeep email CANCELLED. **Still open (separate, non-blocking):** the AUDIT-M3-001 seam-convention question (4 tensors deviate from his public script; `output_blocks.0/1` positional-vs-ranked; `output_blocks.2.0.in_layers.2` weight-positional/bias-ranked) — ask later, not bundled with anything.
2. **M1 GPU acceptance (only remaining M1 item, blocked on GPU + CG):** apply the minimal upstream patch (`setup_peft`/`build_peft_optimizer`/EMA/resume per `docs/integration_notes.md`), then run several hundred real optimization steps and record VRAM, sec/step and a resume test in `docs/compute_budget.md`. M1 CPU acceptance is COMPLETE (M1-005/006/007; F1–F8 all addressed).

## RUN RECIPES

* Pre-phenomenon-data (PHENOM-STAT-D) tests/scripts (all `OPENBLAS_CORETYPE=Haswell .venv/bin/python …`, exact commands in ledger PHENOM-STAT-D): `tests/research/{test_phenomenon_statistic,test_cluster_bootstrap,test_manifest_validator,test_sliced_adapter}.py`; `scripts/research/{build_sliced_adapter.py --verify-p1, verify_sliced_adapter_backbones.py, gate0_rescore_control.py, phenomenon_score_verdict.py --self-test}`; GPU entry (do NOT run in Studio) `bash scripts/research/run_phenomenon_gen.sh`.
* Full resume context: `docs/HANDOFF.md`

* Environment: `.venv/bin/python` (CPython 3.10.20). Rebuild: see `docs/environment_report.md`.
* Model-loading smoke test: `.venv/bin/python scripts/research/smoke_load_unet.py`
* M2 conditioning-path tests (CPU): `.venv/bin/python tests/research/test_conditioning_paths.py`
* M2 evidence (norms, cosine, timing, figures): `.venv/bin/python scripts/research/m2_condition_swap.py --n 48`
* M3A diagnostic machinery tests (CPU, control models): `.venv/bin/python tests/research/test_diagnostics.py`
* A1 real-latent evidence (scale_factor, VAE divergence, schedule): `.venv/bin/python scripts/research/m3a_latent_check.py`
* Build/verify disjoint val split: `.venv/bin/python scripts/research/build_val_split.py`
* M3A random-mask tests (CPU): `.venv/bin/python tests/research/test_random_masks.py`
* M3A matched-null tests (synthetic): `.venv/bin/python tests/research/test_matched_null.py`
* Persist random-null record (seeds+sha256): `.venv/bin/python scripts/research/build_random_null.py`
* R5 bit-exact check vs published L1 ckpt (equality only): `.venv/bin/python scripts/research/verify_l1_bitexact.py`
* Dataset load smoke test: `.venv/bin/python scripts/research/smoke_load_dataset.py --split train --n 2`
* Upstream dataset/checkpoint validation: `.venv/bin/python tests/validate_dataset_checkpoint.py`
* Fetch + md5-verify public artifacts: `bash scripts/research/fetch_public_artifacts.sh`
* Verify structural budget from checkpoints (CPU): `.venv/bin/python scripts/research/verify_pruned_architecture.py data/checkpoints/audioldm-m-full.ckpt data/checkpoints/l1_audioldm-m-full_p1.ckpt`
* Verify adopted P0 convention reproduces the published L1 kept-set: `.venv/bin/python scripts/research/verify_p0_convention.py`
* Verify the L1 pruning direction finding (Spearman -1): `.venv/bin/python scripts/research/verify_l1_direction.py`
* M3B Gate-B overlap tests (CPU, synthetic): `.venv/bin/python tests/research/test_overlap_gate_b.py`
* M1 PEFT backward on the real U-Net (CPU, needs the pruned ckpt): `.venv/bin/python tests/research/test_peft_backward_real_unet.py`
* Verify the Gate-B geometry / finding G1 on the real artifact: `.venv/bin/python scripts/research/verify_gate_b_geometry.py`
* Meeting figure page (presentation only, no new science): `OPENBLAS_CORETYPE=Haswell .venv/bin/python scripts/research/build_meeting_figures.py` -> `artifacts/meeting/modality_result.html` (gitignored)
* Review our patches to upstream: `git diff upstream-frozen -- audioldm_train/`
* SA3 Step 0 (CPU, `.venv-sa3`): `.venv-sa3/bin/python scripts/sa3/step0_verify_pair.py --base-dir data/sa3/small-sfx-base [--post-dir data/sa3/small-sfx] --build --out artifacts/sa3/step0.json`; tests: `.venv-sa3/bin/python tests/sa3/test_blockskip.py`, `tests/sa3/test_loading_diff.py`
* Agent kit verification: `python3 .claude/verify_agent_kit.py .`
* Housekeeping: `python3 .claude/hooks/check_progress.py PROGRESS.md`; `git status --short --branch`

<!-- FIN-ESTADO -->

## LOG

### 2026-08-27 03:27 | ✅ GATE 0 PASSES — dense LoRA uplift replicates on M-Full (ΔCLAP 0.0464, CI95 [0.0221,0.0720])

* Generation `gate0-gen-1` COMPLETED clean (0.8844 cr, 384 dense±LoRA). Verdict (`gate0_score_verdict.py`, fused-CLAP CPU, prompt-cluster bootstrap B=10000): **PASS** — point 0.0464 ≥ SESOI 0.025 AND lo 0.0221 > 0. base cos 0.197→adapter 0.243; per-seed Δ 0.034/0.052/0.054; 68.8% prompts positive; median prompt Δ +0.038; no pathology. Verdict md5 62cefa0b.
* Gate 0 = train 1.7969 + gen 0.8844 = 2.6813 cr; total spent 2.8648 / 5.0 (remaining 2.1352). **STOPPED — falsifier NOT auto-launched; awaiting authorization.** Falsifier build gap: sliced-adapter (mask-induced transfer) generation path for pruned/recovered backbones.

### 2026-08-27 02:08 | Gate-0 TRAINING COMPLETED (19,400 updates, adapter 84a24a38, 1.797 cr); GENERATION job launched

* `gate0-train-1` COMPLETED clean (normal sys.exit -> Lightning Completed; validates the termination fix). 19,400 updates, ALL_ASSERTIONS_PASS, adapter sha 84a24a38 (128 tensors), commit e349d69/dirty=false, final_lr 8.02e-6, losses 0.65->~0.09. Settled 1.797 cr (~1.9 h). Adapter + resume ckpt retrieved bit-exact.
* `gate0-gen-1` launched (T4, dense +/- LoRA both-mode, 384 gens, ~0.68 cr, watchdog 1.0/75min). On completion -> CPU CLAP score -> Gate-0 verdict -> STOP+report. Falsifier NOT auto-launched. Spend 1.98/5.0 cr.

### 2026-08-27 00:30 | Gate-0 launch prereqs resolved (CPU); TRAINING JOB gate0-train-1 launched (T4, FP32, 19,400-update recipe, e349d69)

* Resolved all launch prerequisites (0 GPU spend): output persistence (`/teamspace/jobs/<name>/artifacts/.../artifacts/...` is Studio-readable — confirmed on smoke + completed jobs); clean termination (normal `sys.exit`; smoke's idle hang was the added `os._exit`); generator CUDA+adapter+both (9f09b1b, synth-adapter validated, adapter changes output); prereg rename `p1_pruned_ema_reconstructed` (v4, tests 8/8); trainer resume checkpoints + commit-stamp (e349d69, round-trip PASS, dry-run PASS).
* **Launched `gate0-train-1`** (on-demand T4, FP32, exact recipe, resume every 10 ep), watchdog max-cost 2.5 / max-minutes 175, projected ≈1.474 cr / ~1.75 h. On completion → retrieve adapter → generation job (384 dense±LoRA) → CPU CLAP → Gate-0 verdict → STOP+report. Falsifier NOT auto-launched.
* Flagged untracked stray `scripts/research/build_meeting_figures.py` (old M3/M4 presentation script, not mine) — left in place.

### 2026-08-26 19:28 | DECISION-V4-13 administrative budget amendment (top-up ~10 cr); on-demand FP32 Gate 0 authorized ≤5.0 cr; NO scientific change

* Gabriel: account topped up ~10 cr. Prior STOP-0 FAIL was budgetary-only under the old 1.0-cr feasibility ceiling → now obsolete; old 3.0 effective cap obsolete (5.0 premise changed). Scientific recipe UNCHANGED; nothing changed because of the smoke. Balance query recorded honestly (endpoint `balance=5.0` static, `total_spent` 55.80; ~10 not exposed by SDK — no fabricated reading). `compute_budget.md` + ledger DECISION-V4-13.
* Re-cost (measured): Gate 0 2.152 cr (1.474 train + 0.678 gen); reuse 384 dense±LoRA WAVs if Gate 0 passes; falsifier 768 gens = 1.356 cr; central chain ≈ 3.51 cr < 5.0 authorized.
* Path: on-demand T4 + FP32 + exact 200-ep recipe (no interruptible/fp16/shorter/fewer/probe). Termination verified via prior jobs (normal sys.exit + num_workers=0 → Completed; smoke hang was the added os._exit). Remaining launch prereqs (no GPU): output persistence, generator CUDA/adapter CLI, resume checkpoints, orchestration dry-run.

### 2026-08-26 19:11 | Gate-0 T4 smoke MEASURED — STOP-0 TRIPPED (job gate0-smoke-1, 0.1835 cr, commit 8850417)

* First real GPU numbers for the Gate-0 recipe (dense M-Full + LoRA, FP32, EMA, latent 96): **train 0.3074 s/step** (VRAM 5.38 GB), **gen 7.143 s/clip** @50DDIM/2.5/eta0 (VRAM 5.34 GB). Preflight passed (commit-pinned, dirty=false, T4). Raw `artifacts/icassp_gate0/smoke_t4_measured.json` md5 `cd956ee4…`.
* **Projected Gate-0 = 2.152 cr (train 1.474 + gen 0.678) > 1.0-cr STOP-0 ceiling → FAIL.** Fits the 3.0 cap alone but Gate 0 + falsifier exceed the 3.0 spendable. Per the frozen rule: report + stop, no implicit fp16/recipe rescue. **Gate 0 NOT launched.** Levers (interruptible ≈1.08 cr / shorter horizon / mixed precision / fewer gens) are Gabriel's explicit call. `compute_budget.md` + ledger GATE0-SMOKE-T4-RUN updated.
* Ops: settled 0.1835 cr; job idled ~40 s after `os._exit` (status Running→Pending) → watchdog stopped it at the 12-min ceiling (measurement already complete). Real Gate-0 job needs cleaner termination.

### 2026-08-26 18:44 | GPU smoke authorized; gen-warmup benchmark fix + preflight/provenance hardening; V4-12 pre-recovery renamed (0 cr this turn)

* Gabriel authorized the single ≤0.1-cr T4 smoke (fp16 OUT; FP32 only) after ONE required fix: `gate0_smoke.py` generation timing now discards 1 warm-up gen (same production `generate()` recipe) before timing — CUDA/DDIM init no longer inflates sec/clip. Complete-file CPU dry-run PASS (`gen_warmup_discarded:1`, `provenance` block, exit 0).
* Hardened the smoke as a paid-measurement guard: `--expect-commit`, `--expect-gpu T4`, dirty-tree refusal, `provenance{commit,branch,dirty}` in the JSON, `os._exit(0)` after persisting (idle-billing lesson SA3-SMOKE-T4-001). Compute path unchanged.
* Recorded (non-blocking) the V4-12 naming correction — pre-recovery = `p1_pruned_ema_reconstructed` (L1 selection on the DENSE EMA; published p1 stored EMA is stale) — and narrowed the primary recovered-backbone claim to published-recovered-EMA vs dense-EMA (skeleton §3/§5). Prereg label rename queued to v5 (before the falsifier). `gate0_generator.py` CLI CUDA/adapter TODO logged. Launch = T4 Job + `job_watchdog.py`; rate 0.89 cr/GPU-h. Gate 0 will NOT auto-run.

### 2026-08-26 13:16 | KIM-CLIP-LENGTH audit refutes "256-fixed"; balance = 5.0 cr; §H caption sources fetched (0 cr)

* KIM-CLIP-LENGTH executable CPU audit (`scripts/research/kim_clip_length_audit.py`): the premise that M-Full is locked to a 10.24-s latent is FALSE — `latent_t_size` bears no weight; U-Net strict-loads 690/690 at any length; VAE time-downsample ×4; only constraint = latent_t divisible by 8. Exact 4.0 s (latent 100) crashes; **true near-4-s at latent_t=96 (3.84 s) is feasible, loss well-defined, pruning/LoRA mapping time-independent.** Verdict **A (3.84 s) PROPOSED**; prereg YAML left frozen pending Gabriel's decision.
* Balance resolved via lightning_sdk: **5.0 cr** (2026-08-26 16:27 UTC), recorded in `docs/compute_budget.md`; supersedes stale ~9.6.
* §H (CPU, independent of clip length): fetched MusicCaps CSV (5521 rows) + Kim-193 parquet (68 MB) to gitignored `artifacts/icassp_gate0/`; re-verified cluster-bootstrap 8/8 and Zenodo 21977996 recovered-(1,2,3,1) metadata (live API, not downloaded). Manifest freeze + fused-CLAP scorer = next unit; no GPU.

### 2026-08-26 02:21 | ICASSP publication pivot: Scenario-B math core verified; decision memo in progress (0 cr)

* Directive (Gabriel, via external-reviewer exchange): optimize for a defensible ICASSP-2027 submission by 2026-09-16; phenomenon (differential fragility) before any method; mandatory Gate 0 (replicate an independently published AudioLDM adapter uplift, ≤1 cr) before any pruning severity; first falsifying chain ≤2.5 cr; central figure by ~Sep 10; severity sweep proposed DEFERRED; **no GPU until the amendment is reviewed**.
* Built + verified (CPU, 0 cr): mask-induced LoRA transfer core — `research_pruning/lora_mask_transfer.py` + `tests/research/test_lora_mask_transfer.py` 6/6 (slicing identity exact for Linear+Conv in fp32/fp16; nested-ladder composition exact; invalid kept-index vectors rejected). With Q4/P4 (published pruned ckpt bit-exact pure selection of dense), the Scenario-B transfer is exact-by-construction wherever backbone seams are pure index selection.
* In flight: three audits (M-Full seam inventory incl. attention/proj layers; Kim et al. Appl. Sci. 2025 recipe/weights availability + `s-full-v2` vs M-Full mismatch costing; novelty collisions with priority on any published mechanical LoRA-mapping-across-pruning-masks). Memo + amendment land in the next commit of this unit.

### 2026-08-26 03:02 | DECISION-V4-09 ADOPTED; frozen prereg + statistical core built (CPU, 0 cr)

* Amendment rev4 status PROPOSED→ADOPTED (DECISION-V4-09); authorization now = §H only. Two implementation details frozen at adoption in `configs/research/icassp_gate0_prereg.yaml` and amendment §C: **CI** = one cluster percentile-bootstrap definition everywhere (two-sided 95%, B=10000, seed 20260826, resampling unit = prompt, 3 seeds clustered within prompt); **sample size** = exactly 64 prompts × 3 paired seeds. SESOI/batch/epochs/rank/α/LR/filter/scorer are fixed; smoke measures execution/cost only.
* **`research_pruning/eval/cluster_bootstrap.py`** — the single CI implementation (Gate-0 ΔCLAP verdict; dual severity gate: standalone non-inferiority upper-CI95[E]≤0.025 AND fragility F point≥0.025 & lower-CI95>0). **`tests/research/test_cluster_bootstrap.py` 8/8 PASS**: constant/determinism/coverage(~0.94); prompt-clustering gives a correctly WIDER CI than the n=192 flat misuse (0.042 vs 0.024); Gate-0 pass/weak/noisy; dual gate distinguishes phenomenon from generic capacity loss; **prereg YAML == code constants**.
* **Mid-turn correction:** Arshdeep README updated 2026-08-17 to a NEW Zenodo record **21977996** ("Pruned and finetuned Models"), possibly superseding the pruned-only 21376822 → our "recovered checkpoint not public" claim may be stale. **Arshdeep email PAUSED**; Zenodo 21977996 inspection in flight; if recovered (1,2,3,1) is public we cancel the email and correct provenance. **Guitar Tone Morphing** recorded NOT a ready positive control (no released LoRA weights + private dataset; S-Full-v2 not M-Full) — supporting evidence for GO-AUDIOLDM, not a branch.
* Trainer/gen/scorer stack-map agent in flight → next unit wires the Kim-recipe M-Full trainer + fused-CLAP scorer + mask-transfer path + smoke by reuse. No GPU launched; 0 cr.

### 2026-08-26 02:58 | ICASSP amendment rev4: GO-AUDIOLDM accepted; five reviewer-facing corrections applied (0 cr)

* Gabriel ACCEPTED `GO-AUDIOLDM` as the working thesis but did NOT adopt 8bdad81 verbatim; required five binding corrections before DECISION-V4-09. Applied to `docs/publication_decision_memo.md` §5 + `docs/master_plan_v4_amendment_icassp.md` (now **rev4**):
  1. **Dual phenomenon gate:** standalone non-inferiority `upper-CI95[E(s)]≤0.025` AND differential fragility `F(s)=D(s)−E(s)` with `point≥0.025 AND lower-CI95>0`. F positive but non-inferiority failing = descriptive only. Claim narrowed to **standalone text-audio semantic alignment (CLAP)**; FAD/KAD corroborative, cannot rescue.
  2. **Statistical unit = prompt:** per-prompt paired effect from 3 matched seeds, then **cluster bootstrap over prompts** (never 192 clips as independent). Applies to Gate 0 + every severity.
  3. **Held-out Gate-0 battery:** ~64 hip-hop MusicCaps captions deterministically sampled OUTSIDE the 193 training captions, rule frozen before the smoke + dedup check. 200-epoch language corrected (first published/affordable operating point, not "best band"; reproduction not exact replication). Batch=2 locked; no auto-shrink to fit ceiling → STOP-0.
  4. **Falsifier vs completion tranche split:** {dense,−23.7%,−65%}×{±LoRA} ≤2.5 cr stays the cheap falsifier; if it passes, completion tranche (adapter B independent replicate + full 5-pt ladder) is re-costed post-smoke and **prioritized over the Scenario-A oracle**. Don't raise overall spend now.
  5. **Recovered-backbone baseline:** zero-cost draft request to Arshdeep ready (`docs/arshdeep_recovered_checkpoint_request.md`); if recovery restores standalone but not legacy-adapter utility → stronger story; time-boxed, else pre-recovery limitation stated.
  + Novelty wording softened to "we found no prior work…".
* No code changed; `test_lora_mask_transfer.py` + `test_scenario_b_seam_transfer.py` still green. Adoption of rev4 as DECISION-V4-09 pending Gabriel. 0 cr; GPU stopped.

### 2026-08-26 02:47 | ICASSP pivot COMPLETE: memo verdict GO-AUDIOLDM + amendment proposed + per-seam executable proof (0 cr)

* **All three audits done and folded into `docs/publication_decision_memo.md`; verdict `GO-AUDIOLDM` (§5); `docs/master_plan_v4_amendment_icassp.md` PROPOSED (DECISION-V4-09 pending Gabriel). NO GPU.**
* **Seam proof executable:** new `tests/research/test_scenario_b_seam_transfer.py` **S1–S6 PASS** — 238 changed tensors identical at (1,2,3,4)/(1,2,3,1), every seam deterministically mapped (27 ranked / 211 positional), sliced-LoRA identity exact on all 67 Linear + 33 Conv seams, 138 1D aux tensors consistent, (1,2,3,3) `skip_connection` drop detected (deterministic drop rule), flattened `LoRAConv2d` layout round-trips. Zero learned parameters at any seam.
* **Kim et al. decisive facts:** LoRA weights NOT released anywhere (empty HF repo, placeholder push script); dataset public (193 clips, Apache-2.0); full Table 6/7 fetched — CLAP trajectories swing ±0.11 across checkpoints, 400-epoch column entirely below baseline, protocol = **1 fixed prompt × 5 seeds** ⇒ published uplift compatible with evaluation noise ⇒ Gate 0 designed as OUR powered replication (frozen 64-prompt × 3-seed battery, paired bootstrap, SESOI +0.025, ≤1 cr, smoke-anchored). 200-epoch replication point sits inside their own best band (r8/α16: 0.6941@200 vs 0.6995@800).
* **Novelty audit:** no direct collision; nearest = CA-LoRA (training-based reuse on compressed LLMs), ASSP (slicing with adapter visible), LoRA-X/ProLoRA (cross-model), SVDQuant (quantization, shape-preserving), X-Adapter (trained bridge); differential fragility unmeasured in any domain; FLUX-lite/SSD-1B community breakage = documented motivation; ecosystem honesty: AudioLDM n=1 LoRA, MusicGen ~196.
* **Chain pre-registered in the memo:** smoke ≤0.1 → Gate 0 ≤1 → phenomenon test (D(s)−E(s) ≥ 0.025, paired lower-CI95>0) → oracle baseline; total ≈2.4–3.3 cr, **effective cap 3.0 cr** (min(3.5, 5.0−2.0), DECISION-V4-10), STOPs 0–3 terminal. Severity sweep proposed DEFERRED.

### 2026-08-23 22:12 | rc1.4 task gate: controls FAIL ΔT_AA → STOP RQ2 (over-claim retracted)

* Gabriel: field is NOT a PASS; only ΔT sustains the functional claim; all 3 §5.1 conditions mandatory. **Retracted** "rc1.1 vindicated"/"near-certain". rc1.4 FROZEN (`rq2_validation_protocol.md §11`): primary **T_AA = paired held-out CLAP audio-audio** (8 eval clips, own prompt; paired bootstrap seed 20260824/B=10000); external panel **G_ext(b)=3 lowest-D_P (N=32) excl b** = {11,12,13}/{11,12,14}; task gen 8-step cfg7. Machinery `score_taa.py`/`control_task_gen.py`/`control_verdict.py`/`task_control_verdict` (tests+selftests PASS).
* **RESULT (job `sa3-taa-gen-1` 0.1810 cr, 152 gens, scored 0-GPU):** **both controls FAIL.** host-collapse cond3 PASS (ΔT(post^{−b})≈0, identity), ≥1 external cond4 PASS — but **no dense-transfer uplift** (base+post ΔT_AA CIs contain 0; L13 base slightly negative). Field effect ~1% of ‖F_P‖² → measurable but NOT a CLAP task uplift = the **field≠function gap** the gate was built to catch. **STOP RQ2, no ecological adapters** (pre-registered). Caveat (Gabriel's call): single-block controls may be too weak to be valid task-level positive controls; n=8 wide CIs. `configs/sa3/adapters/control_verdict.json`. Budget **SA3_TOTAL 3.369, headroom 1.63**. STOP GPU.

### 2026-08-24 00:34 | Controls L6+L13 TRAINED (option c) + FIELD evaluation PASSES; task-ΔT remaining

* **Option (c) paired training (SA3-CONTROL-PAIRED-002):** one T4 job (`sa3-controls-1`, **0.2608 cr**, Completed, watchdog not triggered) trained L_6 (block 6, |B|=2.01) + L_13 (block 13, |B|=1.86) as TWO independent 1000-step trainings from pristine base (child `os._exit`, parent clean). Both verified. Discarded incomplete `sa3-l6-1` (0.1557). `train_controls_paired.py` (CPU dry-run PASS).
* **FIELD control evaluation (SA3-CONTROL-EVAL-FIELD-003, `sa3-aeco-1` 0.2747 cr):** A_eco over 20 blocks on 64 states. **Both controls PASS the field portion:** precision_ok (L6 15.5× / L13 7.5× the 10η floor); **A_eco(host)=1.0000 exact** (δF^{−b}=0, largely algebraic since the LoRA lives in that block); **host NOT top-1** (L6 rank10, L13 rank9 — blocks 0/1/17 dominate) → **FAVOURS the rc1.1 no-top-1 decision, NOT yet vindicated** (only demonstrated if the task gate also passes). ΔT(post^{−b})=0 algebraic. `configs/sa3/adapters/control_field_eval.json`.
* **NOT a control PASS — field is mechanistic evidence only; ONLY ΔT sustains the functional claim.** Task gate (rc1.4) mandatory: all THREE conditions incl. observability of a measurable uplift under an external removal. A big field change can yield ZERO semantic-metric improvement (why the chain is A_tan→A_eco→ΔT, not just A_tan→A_eco). Needs a CLAP **audio–audio-to-eval_L** scorer (`T_AA`, primary) + one gen job. **STOP GPU.** Budget **SA3_TOTAL 3.188, headroom 1.81**.

### 2026-08-22 18:40 | Freesound sourced + manifests + L6 train KILLED at 0.12 ceiling (1000 steps ≠ ≤0.12 on T4)

* **Pre-data patch (SA3-FREESOUND-STREAM-002):** stream-sourcer with N-after-all-filters+backfill (shared `clip_accept`), auto `prompts_L`, sourcing provenance, `preview-hq-mp3` frozen (source vs fetched SR). Tests T1–T4 + selftest PASS.
* **Data (SA3-SOURCE-001):** sourced `impact_percussion` + `water_liquid` (40 accepted each, 0 rejected); manifests schema /2 (train32/eval8, 9 auto prompts); **invariants ALL PASS**; provenance tracked in `configs/sa3/adapters/`, audio gitignored. Both ≥20 → GO condition met.
* **L6 attempt (SA3-CONTROL-L6-ATTEMPT-001):** trained on impact train_L; **watchdog worked** and STOPPED `sa3-l6-1` at the 0.12 ceiling — but **1000 steps cost ≈0.15–0.20 cr on T4 (settled 0.1557, INCOMPLETE, no checkpoint)**. The two authorized params (1000 steps / hard 0.12) conflict; I overrode neither. **STOP GPU, no L13.** Budget: **SA3_TOTAL 2.634, headroom 2.37**. Decision needed: raise ceiling ~0.20 / cut steps ~500 / one job for both (amortize provisioning).

### 2026-08-22 18:01 | Budget reconciled from settled costs + teardown fix for real training (0 compute)

* Gabriel: cap 5 = TOTAL Lightning (GPU+CPU); teardown fix must cover real L6/L13, not just `--smoke`. Ledger SA3-BUDGET-RECON-001.
* **Reconciliation** (`scripts/sa3/reconcile_budget.py` from settled `total_cost` of 9 SA3 jobs → `docs/sa3/budget_reconciliation.md`): **SA3_GPU=2.4255, SA3_CPU=0.0526, SA3_TOTAL=2.4781 → headroom 2.5219 cr**. Earlier "2.23"/"2.4435 GPU all-time" were off (2.23 ~0.035 low; 2.4435 mixed base+round GPU+CPU) — superseded.
* **Teardown hardened:** `WANDB_MODE/DISABLED` set before the training stack imports wandb (its non-daemon threads caused the idle-billing) + `os._exit` on any non-dry run (real training AND smoke). New `scripts/sa3/job_watchdog.py` polls **cost** and STOPs on `--max-cost`/`--max-minutes`. Import-checked green. Ops: no separate CPU-check Lightning jobs (local/Studio for free checks); budget **0.08–0.10 cr/control**. **STOP compute until the Freesound token.**

### 2026-08-21 19:10 | T4 synthetic infra micro-smoke — infra GREEN, but OVER the 0.10-cr ceiling (0.2135 cr)

* Gabriel authorised a synthetic-data T4 `L_6` micro-smoke (infra-only, ceiling 0.10 cr). Ledger SA3-SMOKE-T4-001. CPU-check job first (`sa3-smoke-cpucheck-1`, 0.053 cr, Completed) verified the job env (matplotlib/wandb in the external `/system` venv + data/state access). Then T4 (`sa3-smoke-t4-1`, 25 steps, 10 s, `16-mixed`+fp16).
* **Results (`artifacts/sa3/train_smoke_t4.json`):** T4; **sec/step 0.253 s**; **peak VRAM 1.81 GB** (no OOM); loss finite; max|lora_B| 0.291; **export→reload→effect nonzero** (δF²=0.0043). **Projection @0.89 cr/h: ~0.066 cr/control; L6+L13 ≈ 0.132 cr.** Patch-4 T4 fp16 validated on hardware.
* **⚠ CEILING BREACH (disclosed):** total **0.2135 cr** (~2.1x) > 0.10 ceiling. Compute was ~35 s (~0.06 cr) but the T4 job **hung in teardown and kept billing** (wandb non-daemon threads block clean exit) until I stopped it. **Fixed:** `--smoke` now `os._exit()`s after flushing. Lesson: poll cost + kill when result JSON appears. All-time GPU **2.23→2.4435 cr** (cap 5). **STOP GPU.** Full controls stay blocked on a real Freesound token; synthetic results enter no science.

### 2026-08-21 18:52 | rc1.1 — 5 pre-data/GPU fixes + Freesound fetch encoded; BLOCKED on API token (0 GPU)

* Gabriel's review of `9b56b71`: direction accepted, 5 fixes before data/GPU (ledger SA3-RQ2VAL-RC1_1-003). **(1)** prediction gate now uses **pair-specific floors** `F_A=max(f_Atan,f_Aeco)`, `F_D=max(f_DP,f_Aeco)` (single shared floor was wrong; `δ_A<δ_D` is descriptive-only). **(2)** control STOP from **measured CIs** (no `0.10`/`0.02` constants). **(3)** A_eco precision guard reads the **frozen η** from the smoke (`--eta-config`, pooled `max=6.667e-5`), no silent default; driver persists per-prompt `num_by_block`. **(4)** T4 training **`16-mixed`+base fp16** (not bf16). **(5)** intra-domain selection frozen (`docs/sa3/freesound_selection_spec.md`): queries/tags/sort/N/duration/silence/dedup/**derived captions**/**resample-to-44.1**; training crop **10 s**. Manifest tool → `.venv-metrics` (librosa). `fetch_freesound_domain.py` encodes the rule.
* **Validated (CPU):** tests A1–A5/T1–T3/metrics PASS; manifest `--selftest` PASS; dry-runs green (train L_6 10 s fp16 → A_eco field w/ eta provenance → ΔT_L task manifest). **BLOCKER:** Freesound API needs a token (HTTP 401); none in env → **cannot source data**; sourcing/controls/real-data GPU smoke blocked on `$FREESOUND_TOKEN`. Lightning CLI present.

### 2026-08-21 18:18 | Protocol rc1 (6 corrections) + Step-0 CPU machinery built & dry-run green (0 GPU)

* Gabriel's 2nd review: apply an `rq2_validation_protocol` **rc1** patch before any Freesound data, and build ALL Step-0 CPU machinery now. No GPU, no data sourcing. Ledger SA3-RQ2VAL-RC1-002.
* **rc1 (6 corrections, rc0 text preserved):** controls STOP re-spec on δF^{−b}=0 (top-1 A_eco ranking demoted to descriptive); `FD_openl3` descriptive-only; `k=6` primary CONFIRM/CONTRADICT/AMBIGUOUS frozen; `A_eco^generic`+`A_eco^domain` w/ precision guard; per-adapter staging (field A_eco first, ΔT_L only if it survives); uplift ΔT never absolute T. Domains predefined before browsing Freesound.
* **Machinery:** `research_sa3/aeco_predict.py` (pure decision core) + `research_sa3/adapters.py` (load trained LoRA onto base/post) + `scripts/sa3/{train_control_loras,rq2_aeco_driver,build_domain_manifest}.py`. Tests A1–A5 / T1–T3 PASS; manifest `--selftest` PASS; metrics suite no regression. **CPU dry-runs green** end-to-end: train L_6 (9 layers/block 6, non-zero lora_B) → A_eco field driver loads it and the precision guard correctly fires False on the toy adapter → ΔT_L task manifest generates with/without L. Env: training venv needed matplotlib+wandb (uv; `_external/` gitignored). **NOT authorised yet: Freesound sourcing, any GPU/training.**

### 2026-08-21 17:52 | RQ2 pre-gate reframed + GO for the real-LoRA validation phase (CPU, 0 GPU)

* Gabriel’s GO: the N=32 result authorises *validating* whether `A_tan` matches real adapters — NOT RQ3/CASE-C. Three framing corrections to `docs/sa3/round2_results.md` + ledger SA3-RQ2VAL-001: (1) what survived is RQ2’s **structural pre-gate**, not RQ2 (retire “adaptability occupies distinct resources”; `A_tan` is a synthetic proxy until `A_tan→A_eco→ΔT_L`); (2) `{11–16}`/`{9–14}` are **LOO-ranking candidate tails, not sequential-greedy masks** (§3.5); (3) **n_u frozen at 8** (n_u_main=4 diagnostic). Pre-registered the validation phase **before any data/training** (S4): `docs/sa3/rq2_validation_protocol.md` — controls `L_6`/`L_13` first (§5.1, STOP-RQ2 if they don’t localise), then ≤2 ecological CC0 adapters, ΔT_L contract, and the “does A_tan predict A_eco better than D_P?” check. Cap 5 cr (2.23 spent). Next unit: Step 0 CPU (data + `train_control_loras.py` + `A_eco`/`ΔT_L` driver). No GPU.

### 2026-08-21 16:33 | A_tan gate RESOLVED at N=32 — RQ2 SURVIVES (GPU all-time 2.23 cr)  [corrected 17:52: pre-gate, not RQ2 — see entry above]

* κ micro-calibrated (CPU) → frozen 0.01 (all 8 probes linear, precision≥floor; smaller κ fail precision), confirming the N=16 result was not a κ artifact. A_tan at N=32/n_u=8/κ=0.01: removable tail {11–16} vs D_P {9–14}, δ=2 robust at k=6, IDENTICAL across N=16→N=32, D_P stable at N=32. → A_tan carries a stable ranking difference from D_P (**RQ2 structural pre-gate**, corrected 17:52 — NOT “adaptability ≠ sensitivity”; a synthetic proxy, LOO-ranking tails not greedy masks). Next (Gabriel's GO): the real-LoRA validation phase, controls-first (`docs/sa3/rq2_validation_protocol.md`). Ledger SA3-KAPPA-CALIB-001, SA3-ATAN-N32-001.

### 2026-08-21 16:05 | Round-2: sufficient-stats sizing + A_tan gate (GPU 1.68 cr total)

* Applied Gabriel's round-2 review (language fixes + per-prompt sufficient statistics so N_main/n_u bootstrap without re-GPU). Expanded pilot N=64 → N_main(D_P)=32, I_PT redundant+noisier (PT-specific criterion closed). A_tan gate N=16/n_u=16 → n_u_main=8 but UNDERPOWERED (the 1–2 block A_tan-vs-D_P divergence is within D_P's sampling noise; corrected a too-loose floor that had over-claimed "A_tan diverges"). RQ2 unresolved; next single step A_tan at N=32 (~0.9 cr, Gabriel's call). Ledger SA3-REVIEW-CORR-002, SA3-PILOT-N64-001, SA3-ATAN-001.

### 2026-08-21 13:23 | Review corrections (CPU, 0 GPU) — adversary + RQ1 conclusions revised

* Applied Gabriel's 9-point methodological review with 0 GPU, from existing artifacts. Adversary: corrected the seed floor scale (m_CLAP 0.164→0.040) and the latency comparator (nearest=dense-8), and applied a paired-bootstrap 3-way rule → INFERIOR {0,1,6,19}, CLAP-non-inferior {13,14,16}, 13 indeterminate ⇒ N=16 underpowered for CASE-E; "removable" downgraded to CLAP-non-inferior per Gabriel (both prior reads wrong). RQ1: field-norm ratio 0.889 confirms the 10× is real amplification (non-normalized 9-10× interior); I_PT nearly collapses onto D_P (removal-set disagree ≤1, no floor). FD replaced by a pre-registered paired OpenL3 cosine-drift diagnostic (running). Fixed .venv-metrics setuptools/pkg_resources. Ledger SA3-REVIEW-CORR-001; erratum docs/sa3/review_erratum_2026-08-21.md.

### 2026-08-21 04:00 | Overnight mandate COMPLETE — RQ1 pilot + single-block adversary (0.733 cr)

* Executed the full conditional overnight mandate end-to-end within budget (0.733 of 5 cap / ≤2 target). Built + CPU-validated the entire SA3 analysis stack (Phase 0), then ran 3 GPU jobs each after a green CPU dry-run from a clean pushed commit with `--expect-commit` + `max_runtime`: smoke (0.156), RQ1 field pilot N=32 (0.325), single-block E adversary N=16/R=5 (0.216); one pilot job stopped in provisioning (0.036).
* **Scientific (pilot, no §8 decision):** (RQ1) few-step post-training keeps the boundary blocks critical but makes the model ~10× more sensitive to *interior*-block removal than the base, while the parameter delta stays uniform and uncorrelated with function (ρ(D_P,W)=−0.21). (Adversary) single-block removal is fatal only at the 3 boundary blocks; interior single-block removals sit within the R=5 seed margin on CLAP → **CASE E not established** (the n=4 smoke over-read skip-5). Next: a GPU OpenL3-FD pass to complete the multivariate verdict before any CASE-E call. Ledger SA3-PHASE0-001..007, SA3-SMOKE-001, SA3-PILOT-FIELDS-001, SA3-ADVERSARY-001.

### 2026-08-21 03:10 | Overnight mandate — Phase 0 (CPU) + Phase 1 (T4 smoke) complete

* Under Gabriel's conditional overnight mandate (cap 5 cr / target ≤2). Built + CPU-validated the entire SA3 analysis stack: deterministic panels/schedules/seeds; metrics/greedy/samplesize/E-rule decision core; isolated `.venv-metrics` (CLAP/PaSST/OpenL3) + scorer; fields.py (raw/deploy) validated on the real base/post pair; e2e/states/latency (S_traj τ confirmed = frozen schedule). One T4 smoke `sa3-smoke-1` (0.156 cr, 0 errors): fp16 s/forward, η, latency, VRAM, empty-mask bit-exact all measured; E-panel metrics run on real GPU audio; skip-one-block drifts far more than the latency-matched dense comparator (CASE-E hint, P_smoke/not scientific). Accumulated GPU spend tonight: 0.156 cr. Ledger SA3-PHASE0-001..006, SA3-SMOKE-001; compute_budget updated with real T4 numbers.

### 2026-08-21 01:07 | SA3 Step 0 COMPLETE — pair verified

* Post repo fetched (token from the HF token file only; logged out afterwards). Pair verification PASS with real outputs in the ledger: identical DiT config and state-dict geometry, identical T5Gemma copies, effective pretransform scale 1.0 in both (`model.pretransform.scale` is a dead config key — the factory never reads it), strict loads 685/685 on both, BlockMask bit-exact on both, base/post fields differ. Ledger SA3-STEP0-002. No GPU.

### 2026-08-21 00:30 | rc3.1 patch + Step 0 pair tooling (post blocked on HF gate)

* rc3.1: binding execution sequence; adapter band on the uplift `ΔT_L`; E-greedy fallback with CLAP and non-negative floored margins; `N_main` from a pre-registered ladder without extrapolation; 433 M labelled nominal. Step 0 script extended to the post (strict load, BlockMask, classified config diff, t5 hashes, fields-differ check); token-safe fetcher added. Ledger SA3-PROTOCOL-004. No GPU.

### 2026-08-21 00:05 | SA3 Step 0 CPU (base side) PASS; protocol rc3

* rc3 (`a5dd7c0`) applied the second review's eight fixes. Step 0: `.venv-sa3` from the upstream uv.lock (torch 2.7.1+cu126); base repo downloaded and hashed; 685 tensors / 567.6 M params; 20 equal blocks of 22.30 M; strict exact load; tiny forward finite; BlockMask identity bit-exact on real weights; tests V1–V3, K1–K4 PASS. Finding: the base config points its text encoder at the gated post repo — redirected to the bundled copy. Post side waits for the HF gate. Ledger SA3-STEP0-001. No GPU.

### 2026-08-20 23:35 | SA3 protocol rc2 (review fixes)

* Eight mandatory fixes from the external review applied: operational `E` tuple + non-inferiority rule on measured seed floors; trained localized LoRA as the real positive control; `A_tan` (κ-convergent) vs `A_eco` (held-out adapters); backbone-only ecological LoRA (`--include transformer.layers`); sequential greedy non-additive selections; block-pass bracket + latency-matched comparator instead of "iso-compute"; `I_PT` as unbounded normalized distortion with pooled ratio + η guard + per-level table; pre-registered `N_p`/`n_u` rules. Ledger SA3-PROTOCOL-002. Nothing executed.

### 2026-08-20 23:14 | SA3 RQ1/RQ2 analysis protocol written (no GPU)

* `docs/sa3/analysis_protocol_rq1_rq2.md`: forward-only protocol for RQ1 (does few-step post-training change structural importance) and RQ2 (does low-rank adaptability occupy distinct structure) on SA3 `small-sfx-base`/`small-sfx`, built from source-verified facts (velocity parameterisation shared by both checkpoints; callback-captured trajectory states; production APG default; residual blocks ⇒ removal = identity). Decision-term SESOI with measured anchors, two positive controls, adversary-first, frozen A–E matrix, implementation plan with TBM costs. Ledger SA3-PROTOCOL-001. Nothing executed.

### 2026-08-20 22:02 | DECISION-RULES-001 + CAR-LoRA in the novelty ledger

* Gabriel kept R1–R6, R8 as scientific adoption conditions, dropped process rules, added S7 (detectable ≠ valuable; SESOI in decision terms); R1 amended to functional effect estimates. CAR-LoRA (ICLR 2026) verified: adapter-side robustness in LLMs — our gap is backbone-side. SA3 `lora-xs` = `U M Vᵀ` on frozen base-SVD bases; `svd_bases.pt` ships only with the base repo (transfer basis-dependent). Constrained formulation + exact mask enumeration (k ≤ 4) + two positive controls + case E accepted into the draft. RQ1–RQ4 frozen conceptually; no thresholds, no plan adopted.

### 2026-08-20 21:55 | Post-mortem of plans v3/v4 + adoption rules R1–R10

* `docs/review/2026-08-20_postmortem_v3_v4.md` (ledger POSTMORTEM-V3-V4): the gates worked; the failure was upstream — effect size never estimated before adoption (v3's 1.5 % ε signal was measured at M2 and filed as a binary PASS; v4 built on a 3-family table with no uncertainty, adopted in 1 h 43 min with the deadline held invariant, power sim run after adoption). Ten adoption conditions (R1–R10) now gate any new plan; applied to the SA3 direction, R1/R2/R6/R7 are still owed.

### 2026-08-20 21:44 | Pivot assessment 2 (Stable Audio 3 base/post pair) — facts verified, no decision

* Verified: SA3 `small-sfx-base` public (no gate) / `small-sfx` gated-auto, same pair for `small-music`; DiT depth 20, dim 1024, 16 heads; base ~50 steps + CFG, post 8 steps; official LoRA transfer base→post; LoRA training ≈ 2.5 GB VRAM; torch 2.7.1. Prior art 2607.06335 (image, prune→distill needs teacher repair), 2607.06631 (video, sparsify-in-distillation), EcoDiff, T2A-bench (Gemini API) confirmed. Assessment in-session; ledger PIVOT-ASSESSMENT-2. Nothing authorized.

### 2026-08-20 21:30 | Pivot assessment (SAOS few-step × pruning × LoRA) — facts verified, no decision

* Verified externally: SAOS repo gated (community license, research OK), both `base_model.ckpt` and `model.ckpt` present (genealogy unconfirmed); ARC post-training cost 100k it × 256 × 8×H100 (variant prune→ARC out of reach); `stable-audio-tools` supports generator LoRA inside ARC, needs PyTorch ≥ 2.5 (separate venv); ICASSP 2027 deadline 2026-09-16 (27 days). Assessment delivered in-session; ledger PIVOT-ASSESSMENT. Nothing authorized; plan v4 unchanged.

### 2026-08-20 21:00 | Severity-sweep proposal (pre-Gate-E falsification of generic capacity loss) — NOT authorized

* Re-verified Tier-0 from `screen_eval.json`: latent between-event SD 0.122 (35 events) / 0.000 (8 events). **Corrections:** P1-nat does not protect better than P0-std at the pooled level (0.328 vs 0.336); a homogeneous logit shift already produces ≈0.07 mechanical SD, so the structured δ is ≈0.10.
* Verified on CPU that P0-std materializes at all four nested budgets (−23.7/−42.5/−56.2/−65 %). New `scripts/research/severity_sweep_power_sim.py` (+ tests W1–W4 PASS, Type-I 0.04–0.07): on the frozen 50×20 mechanism set an overdispersion test vs a common-logit-shift null has power 0.85–0.89 @ δ≈0.13 — Gate-E-30×30-equivalent power at ~1/10 of the clips. Costed A ≈ 13 cr / A+ ≈ 23.5 / B ≈ 6.7 / C ≈ 26 with pre-registered CLOSE/COLLAPSE/DISPARITY branches (`docs/severity_sweep_proposal.md`, ledger SWEEP-PROPOSAL). Plan v4 unchanged; awaiting Gabriel's decision.

### 2026-08-20 | Tier-0 GPU campaign COMPLETE (3 jobs)

* Authorized 16:00. Executed in order, each with CPU dry-run + GPU smoke + clean pushed commit, T4, 2.0-cr reserve intocable. **Job 1 D1+D2** (`d1d2-1` 0.362): RQ-swap signed asymmetry small/negative, Gate-A screen non-significant (reinforces v3 negative); D2 guidance ΔG≈G. **Job 2 Gate B′** (`gateb-1` 0.457): 5120-slot P1 per-slot store; null half-half overlap 5th-pctile ⇒ threshold 0.9401. **Job 3 screening** (`screen-1` 1.412): heterogeneity screen — event-level forgetting present, criterion-stable (Spearman 0.663), data-aware P1 protects better; supports Tier-1 funding but underpowered (8 events ≥5 prompts).
* **⚠ FINDING-P0-COLLAPSE caught before spending GPU:** M3B saliency artifact's `P0_L1`==`P0_published` (sum-norm cancels ±L1 sign) → M4-SCREEN-FOUND's P0-convention gap was seed noise. Fixed (P0-pub=published pkl, P0-std=reversed pkl) in the D1+D2 and screening runners.
* Final all-time spend **6.9150 cr** (20 jobs), reserve 2.0 intact (~0.69 headroom). Ledger TIER0-D1D2-RUN / TIER0-GATEB-RUN / TIER0-SCREEN-RUN. **No Tier 1 without new authorization.**

### 2026-08-20 | CPU queue Q7: Gate E power simulation — CPU QUEUE Q1–Q7 COMPLETE

* `scripts/research/gate_e_power_sim.py`: parametric bootstrap of Gate E's exact rank test on the between-event loss variance. FP rate at δ=0 = 0.049 ≈ α (Type-I controlled). Frozen 20×15×K_rand=20 design has MDE δ=0.35 for 80% power at placeholder rates (p_base=0.6, μ_loss=0.15) → underpowered for small effects; resize levers quantified (30 prompts→δ=0.20; 40 events→δ=0.25). Concrete MDE/resize fixed once Tier-0 rates land (M4-1b). New `tests/research/test_gate_e_power.py` E1–E5 PASS. Ledger Q7/GATE-E-POWER.
* **CPU queue Q1–Q7 all done.** Next steps require a GPU Job (Tier-0), so they need Gabriel's explicit authorization; nothing runs on the T4 without a clean pushed commit, a CPU dry-run and a GPU smoke.

### 2026-08-20 | CPU queue Q6: H-guidance conditioning + D1 signed per-stratum in the M3A runner

* `scripts/research/m3a_diagnostics.py`: added template (`"a sound of [alias]"`), `c_only_e`, and `c_without_e` (frozen whole-word deletion) text conditioning; `eps_under_text`/`guidance_norm`/`guidance_probe` (uses the Q2 strict synonym map, empty-string unconditional); `d1_signed_per_stratum` (signed `‖E_a‖−‖E_t‖`, `norm_E_a`, `norm_E_t`, `R_mod` overall + per stratum) reported for L1 as `result["D1"]`; `--guidance-probe` flag; slots carry `stratum_idx`/`captions`.
* Pure-function logic unit-tested synthetically; **CPU dry-run PASS** (exit 0): probe deleted a real requested event, `G_tmpl,F=3.77`/`G_only,F=2.79`/`G_event,F=1.61`/`G_caption,F=3.52` finite and ordered, D1 per-stratum present; Gate-A flow unchanged. Ledger Q6/GUIDANCE-D1.

### 2026-08-20 | CPU queue Q5: Gate B′ machinery (per-slot storage + null-split)

* New `research_pruning/paired_modality/gate_b_prime.py`: per-slot saliency storage `{layer:(n_slots,n_ch)}`, CPU `recompose_mean`, `null_split_overlaps` (≥1000 half-splits → weighted kept-set overlaps via `overlap.py`), `gate_b_prime` (PASS iff observed < 5th-pctile of half-half null). `m3b_saliency.py` gained `--per-slot-store` (validated in `--dry-run-cpu`; store re-loads and drives the null-split on CPU).
* New `tests/research/test_gate_b_prime.py` G1–G4 PASS on a control net: recompose==accumulate to FP; null median 0.917 vs chance 0.50; fires on an independent saliency (p<0.001), no-fire on identical (p=1.0). `test_overlap_gate_b`/`test_taylor_saliency` still PASS. Ledger Q5/GATE-B-PRIME.

### 2026-08-20 | CPU queue Q4: materializer parameterized by channel_mult

* `research_pruning/diagnostics/random_masks.py` gained an optional `channel_mult` (default `(1,2,3,1)`) on `build_pruned_unet`/`materialize`/`kept_counts`/`ranking_driven_layers`/`build_random_null`, plus `count_pruned_params`. Backward-compatible: all existing callers and the R5 golden path are unchanged.
* New `tests/research/test_materialize_channel_mult.py` P1–P4 PASS — exact param counts (145,673,864 default; 317,308,040 at `(1,2,3,4)`), mild-budget materialize strict-loads+forwards, and R5 690/690 bit-exact via explicit `channel_mult=[1,2,3,1]`. `test_random_masks.py` R1–R5 still PASS. Ledger Q4/MATERIALIZE-PARAM.

### 2026-08-20 | CPU queue Q3: FineLAP CPU smoke PASS → V4-06 resolved (H-acoustic stays)

* `scripts/research/finelap_smoke.py` loads `AndreasXi/FineLAP` (MIT) on CPU under torch 1.13.1 via two documented env-gap adapters (backport transformers' nested-config recursion; load as a local package instead of trust_remote_code, whose 4.30.2 dynamic-module copier misses the auxiliary .py). Neither relaxes a pin nor changes FineLAP/numerics.
* Frozen occupancy rule `mean_t[sigmoid≥0.5]×10.24s`. On 5 curated clear single-label clips, own-event grounding beats disjoint distractors 5/5 (1-2 orders), and occupancy separates brief (Gunshot 0.96s, Sneeze 0.80s) from sustained (Applause 10.24s, Whistling 8.32s, Bell 7.52s). Verdict PASS → H-acoustic stays in the primary Gate M. First auto-selected set had abstract events (Vibration/Gurgling)→3/5; harness corrected to clear events (recorded transparently). Ledger Q3/FINELAP-SMOKE; DECISION-V4-06 resolved.

### 2026-08-20 | CPU queue Q2: plan-v4 pre-registration manifests frozen + hashed

* `scripts/research/build_v4_manifests.py` builds nine manifests deterministically (`--check` bit-identical) from frozen inputs only (labels CSV, train captions, fetched AudioSet ontology, M3B calibration manifest); no model/GPU, no pruned generation inspected. Master seed 20260818.
* Strict requested-event map validated against REVIEW-004 (exact on Siren/Gunshot/Explosion/single-label/Speech-labelled). E*=101(T0)/98(T1). Partition (calibration 512, mechanism 999, holdout 500 blinded, sentinel 300, screen 200) pairwise-disjoint at wav id and disjoint from the val split (0 overlap). All sha256 recorded in the ledger (Q2/MANIFEST-FREEZE) and `configs/research/v4_manifests_index.json`.

### 2026-08-20 | CPU queue Q1: FAD/FD NaN fixed + E-BLAS environment defect found & fixed

* Fixing the M4 screening FAD/FD NaN (Q1) surfaced **E-BLAS**: numpy 1.23.5's bundled OpenBLAS 0.3.20 silently returns wrong matmul/eigh/svd/cov on this Xeon 8488C (Sapphire Rapids) for n>64 — the Frechet NaN was a symptom, not just F-eval-3. Root-caused (numpy ILP64 0.3.20 vs scipy's correct 0.3.18), fixed with `OPENBLAS_CORETYPE=Haswell` (startup `.pth` via `scripts/research/install_blas_fix.sh` + `research_pruning/eval/_blas.py` raise-guard). Documented in `docs/environment_report.md`.
* Built a tracked, real-part, eigenvalue-based Frechet (`research_pruning/eval/frechet.py`) and re-scored all 6 screening systems from the existing feature caches (no re-generation, no GPU): finite FAD/FD for all, self-distance ≈0, IS reproduces M4-SCREEN-FOUND to 5 dp (same audio). base best (FAD 0.945/FD 73.8), P3 worst — screening-only, `claims_matrix` unchanged. Ledger E-BLAS + Q1/M4-SCREEN-RESCORE.
* Full research suite **14/14 modules PASS** with the fix (incl. new `test_frechet.py` T1–T6). Follow-up: spot-check prior CPU numpy linalg on >64-dim matrices (M3A OLS expected safe).

### 2026-08-20 | DECISION-V4-00..07 recorded; plan v4 adopted as the contract

* Gabriel adopted all eight recommended defaults verbatim (Tier 0 only; balance ~3–5 cr). Recorded one ledger entry per decision (`DECISION-V4-00..07`, "Decided by Gabriel").
* Propagated: `docs/master_plan_v4_draft.md` → `docs/master_plan_v4.md` (status CONTRACT); v3 header marked SUPERSEDED (kept for history); `AGENTS.md` step 2 → v4; `docs/claims_matrix.md` rewritten per §9 (RQ1/RQ2a/RQ2b rejected = RQ-swap closed negative; new RQ1′–RQ4′; SEM folded into RQ1′; RQ4′ `unavailable` until Tier 2); PROGRESS state block + this log.
* No gate result changed; no GPU spent. Next: credit-free CPU queue Q1–Q7 (`docs/NEXT_SESSION.md` §3), one `/cerrar-hito` per item.

### 2026-08-20 | Hostile review round 5 — plan v4 rc4 (freeze candidate)

* Applied Reviewer A's six consistency fixes; verified FineLAP public weights/API; no methodological change. Ledger REVIEW-005. No gate changed.

### 2026-08-20 | Hostile review round 4 — plan v4 rc3

* Accepted Reviewer A's seven identification fixes, verified feasibility on CPU (counterfactual text path, 22 697 single-requested-event clips, 9 637 single-label clips, 61 events ≥200 requests), re-costed Tier 1 ≈45 cr. Ledger REVIEW-004. No gate changed.

### 2026-08-20 | Hostile review round 3 — plan v4 rc2

* Accepted two rc1 errors (Gate E/Tier 0; RAND×2), measured mild-budget geometry on CPU, verified AudioSet aliases and FineLAP availability; wrote round-3 doc and rc2. Ledger REVIEW-003. No gate changed.

### 2026-08-20 | Hostile review round 2 — plan v4 draft

* Verified Reviewer A's round-2 citations (2607.20048, 2607.24731, 2606.17404, Singh Table 3); reproduced the AudioCaps exposure gradient from the local manifest; summed job spend via SDK (4.205 cr).
* Wrote `docs/review/2026-08-20_reframing_round2.md` and `docs/master_plan_v4_draft.md` (tiered by credits; six pending decisions). Ledger REVIEW-002. No gate changed.

### 2026-08-20 | Hostile review round 1 — M4 screening recorded retroactively; reframing draft

* Found `m4-screening-1` results in gitignored `artifacts/` with no ledger entry; recorded as M4-SCREEN-FOUND with its defects (FAD/FD NaN, 1 seed, folder-name caveat, deviation from "run only if M3 passes").
* Verified the second reviewer's 2026 citations by web search (COMET 2605.29628, DASH 2606.00798, OBS-Diff 2510.06751/ICLR 2026, MosaicDiff ICCV 2025, Sparse Guidance CVPR 2026, Arshdeep 2607.13330, Closing the Null Space 2607.08241). Could NOT locate "Importance-Aware OBS Pruning (22 Jul 2026)" — flagged back to the reviewer.
* Wrote `docs/review/2026-08-20_reframing_round1.md`: audit verdict, candidate RQ set for plan v4, cheap pre-gates, and open literature questions. No scientific gate changed.

### 2026-08-18 | Bootstrap of the dedicated research repository

* Searched for the prior research repository before creating anything: `gh repo list` / `gh api /user/repos` (owner+collaborator+org) and a filesystem-wide search returned no AudioLDM, modality-swap, or pruning repository. Repository therefore initialized from scratch, not recovered.
* Installed `audioldm-agent-kit-minimal-v2` against this repository root.
* Verifications passed: `check_progress.py` OK, `verify_agent_kit.py` OK, `settings.json` valid JSON, `bash -n` on all three hooks OK, all three hooks execute cleanly, kit `tests/test_install.sh` self-test OK.
* Contamination audit: repository clean. External scopes clean except for account-level claude.ai MCP connectors (Atlassian/Jira, Gmail, Google Calendar, Google Drive, Slack) reachable from any session in this environment; these are outside the repository and cannot be removed by the kit.
* No scientific milestone is marked complete by this bootstrap.

### 2026-08-18 | M0: upstream import, frozen references, public-artifact inventory

* Imported the full upstream AudioLDM history and pinned both frozen reference SHAs as branches; `audioldm_train/` is byte-identical to `upstream-frozen`.
* Created the master-plan §13 directory structure as empty skeletons only.
* Fetched and md5-verified the public checkpoints; wrote `scripts/research/fetch_public_artifacts.sh` and `scripts/research/verify_pruned_architecture.py`.
* Derived `(1,2,3,1)` directly from checkpoint tensor shapes and proved the published pruned checkpoint was never finetuned, which resolved the full-FT strength gate against public availability.
* Recorded everything in `docs/experiment_ledger.md` (M0-001) and `docs/m0_baseline_reproduction/`; updated RQ3 and EFF wording in `docs/claims_matrix.md`.
* M0 remains open: no environment, no FAD/KL pipeline, no PANNs pipeline, no GPU smoke test. No M1 code written.

### 2026-08-18 | M0: reproducible Python 3.10 environment and CPU load smoke tests

* Built the dedicated environment faithful to `upstream-frozen`: standalone CPython 3.10.20 via `uv` (Lightning blocks `conda create`; `/commands/python3.10` is a decoy that runs 3.12), then upstream's own `pip install poetry` + `poetry install` on the unmodified lock. 155 packages, 0 errors, no pin relaxed.
* Import smoke tests 6/6 pass, including the FAD/KL stack.
* Added `scripts/research/smoke_load_unet.py`: both budgets rebuilt and strict-loaded with 0 missing / 0 unexpected keys, so the M0 deterministic-reconstruction criterion is met.
* Fixed a `torch.load(mmap=True)` incompatibility in our own helper scripts (torch 1.13.1 predates `mmap`). No upstream or scientific code touched.
* Logged as M0-003. `docs/compute_budget.md` untouched: no GPU, nothing measured.

### 2026-08-18 | M0: AudioCaps fetched, upstream validation passes

* All six public artifacts downloaded and md5-verified (`fetch rc=0`); `dataset.tar` extracted cleanly to 31 GB / 50 961 wav.
* Upstream `tests/validate_dataset_checkpoint.py` passes on the first full run: structure complete, all 50 466 referenced audio files present. No fix was needed.
* Added `scripts/research/smoke_load_dataset.py`; train and test splits both load with the expected preprocessing shapes.
* Recorded the val == test trap in `docs/m0_baseline_reproduction/dataset_manifest.md` and `docs/pilot_protocol.md`.
* Logged as M0-004. No scientific code modified; `git diff upstream-frozen -- audioldm_train/` still empty.

### 2026-08-18 | Session close: handoff written

* Wrote `docs/HANDOFF.md` as the single self-contained resume point: repo/HEAD/remotes, frozen SHAs, verified M0 items with evidence paths, remaining M0 work, M1/M2 status, M3 gates, environment and data locations, re-run commands, findings, deviations, a prioritised available-vs-blocked work queue, and a "Do not assume" list.
* Logged as HANDOFF-001 in `docs/experiment_ledger.md`, explicitly as administrative and not an experiment.
* No experiment, download, installation or GPU work was performed during this close-out. No gate resolved: M0 open, M1 absent and blocked, M3 blocked.

### 2026-08-19 | Autonomous night run: M1 scaffold adopted, audit defects F2–F5/F7 fixed

* Gabriel authorised an autonomous ~8h run (00:30→08:30 Montevideo) across the 05:28 token-window renewal; a detached resume daemon (`artifacts/auto_resume/`, gitignored) relaunches headless `claude` at 05:30 to continue.
* Adopted the recovered, audited M1 PEFT scaffold into `audioldm_peft/` and fixed audit defects **F2** (LayerNorm no longer half-trained; explicit flag + separate reporting), **F3** (unconditional counting), **F4** (order-safe freeze + guard), **F5** (factorised LoRA conv), **F7** (full resume state). Added stdlib test runner (**F1**).
* CPU dummy-model tests **L1–L3, J1–J4, S1–S3 all PASS** (**M1-005**). Then **F6** real pruned-U-Net tests R6a–c PASS (**M1-006**) and **F8** integration hooks I1–I4 PASS (**M1-007**). **M1 CPU acceptance COMPLETE** — full suite 17/17 across 5 modules; all 8 audit defects F1–F8 addressed. Only M1 GPU acceptance remains (blocked). No GPU work; `git diff upstream-frozen -- audioldm_train/` still empty.

### 2026-08-19 | Autonomous night run: M0 eval pipelines + GPU benchmark prep

* Prepared the §7.2 GPU benchmark (`scripts/research/gpu_benchmark.py`, **M0-005**) — write-only, refuses without CUDA; `compute_budget.md` unchanged (no numbers invented).
* Exercised both M0 evaluation pipelines end-to-end on CPU (**M0-006**): PANNs top-10 fully reproduced (20/20 clips, coherent labels); FAD/KL runs after resolving the PANNs Cnn14 16k checkpoint dependency + CPU sanitize + a path-keyed cache trap, and working around a genuine `audioldm_eval` FAD bug (documented, F-eval-3). Findings + invocations in `docs/m0_baseline_reproduction/eval_pipeline_closure.md`.
* FAD/KL numeric KL/IS/FID values captured in a follow-up commit once the CPU run finishes. `git diff upstream-frozen -- audioldm_train/` still empty.


### 2026-08-19 | Evidence-first audit pass (05:30 resume run by hand; VM had slept)

* The detached auto-resume daemon (pid 5102) never fired: the Lightning Studio VM slept during the idle window and killed it before the 05:28 token-window renewed (`resume.log` shows only "daemon armed"; the VM cold-booted 12:09 UTC). `setsid` survives the Claude process dying but NOT the VM sleeping — a real gap in the resume design. Gabriel re-ran the pass manually while present.
* Ran the safe pass the resume prompt scheduled: **full research suite 11/11 modules PASS** (exit 0); `git diff upstream-frozen -- audioldm_train/` = 0 lines; evidence-first `/auditar` of the night's machinery. **No bug found; no code changed.**
* **M3B-002 independently re-derived and CONFIRMED** (both sides of the reference code + `verify_l1_direction.py`: Spearman −1 on 28 layers, 15/15 kept-set-lower-L1). Refutation attempts failed. **Not acted on** — the P0-convention choice is Gabriel's. P0-P3 Taylor machinery, LoRA F5 factorised conv (exact), and eval findings F-eval-1..6 all re-derived correct. See ledger AUDIT-NIGHT2.
* Everything still open is a decision or GPU-gated, not a bug. Daemon NOT re-armed (Gabriel present, CPU queue exhausted).

### 2026-08-19 | DECISION-M3B-002: P0 baseline adopts Arshdeep's published inverted-L1 convention

* Gabriel's rule: inverted convention iff the published pruning work is Arshdeep's, else standard L1. Provenance confirmed Arshdeep's official artifact (Zenodo 21376822; reference README "Official implementation") → **adopted the published inverted convention: P0 keeps LOWEST-L1 filters.**
* Implemented `p0_importance(convs, convention)` + `P0_CONVENTION='published'` (default) in `research_pruning/taylor/saliency.py` (`'published'`=-L1 so keep_topk keeps low-L1; `'standard'`=+L1 for non-Arshdeep baselines only). `p0_l1_magnitude` unchanged.
* Verified: `verify_p0_convention.py` reproduces the published kept-set **exactly 12/12 ranking-driven layers** on the real base U-Net; control test **C8 PASS** (Taylor suite now C1–C8). No saliency on the real L1 ckpt; upstream diff empty. See ledger DECISION-M3B-002.

### 2026-08-19 | Gate B amended (G1/G2 resolved), Gate-B statistic implemented, M3B-002 propagated

* Found that `DECISION-M3B-002` (P0 adopts the published inverted-L1 convention) had reached `docs/claims_matrix.md` and the finding write-up but **not** `docs/pilot_protocol.md` — the very document that must be frozen before any saliency is inspected. Propagated it there as pre-registration.
* Surfaced the consequence nobody had recorded: P1/P2/P3 keep the **highest**-saliency channels while P0 now keeps the **lowest**-L1 ones, so a P1-vs-P0 margin confounds criterion *direction* with criterion *quality*. Gabriel decided to report `p0_importance('standard')` as a **secondary reference** (zero extra compute) and pre-registered a mandatory wording constraint ("vs the published L1 pruning artifact", never "vs standard L1").
* **Gate B amended (DECISION-M3B-003, Gabriel, option (a)).** The master plan's Gate B was infeasible as written: at the `(1,2,3,1)` budget prune-set overlap lives in `[0.75, 1.0]` with chance `0.80`, so its `<= 0.70` condition was mathematically impossible (audit finding G1) and the draft carried two contradictory definitions (G2). The gate is now the **kept-set** overlap with the plan's numerals transferred verbatim (weighted `<= 0.80`, `>= 2` ranking-driven layers `<= 0.70`); chance is `0.20` and the range is the full `[0, 1]`. Re-derived from the real artifact — 12 ranking-driven layers, all N=960/k=192/p=768, floor 0.75 on every one (`verify_gate_b_geometry.py`, exit 0).
* **Implemented the Gate B statistic (M3B-003):** `research_pruning/paired_modality/overlap.py`. Gate A had one since M3-001; Gate B had none. Control tests `test_overlap_gate_b.py` **O1–O6 PASS**, including that identity FAILS the gate, independence lands at chance, the prune-set floor matches brute force, both gate conditions are necessary, the thresholds are inclusive and floating-point exact at the boundary, and that leaving the 3 positional seams in dilutes the verdict.
* Full research suite **12/12 modules PASS**; `git diff upstream-frozen -- audioldm_train/` still empty. No saliency computed on any real checkpoint, the L1 checkpoint was not opened, and no GPU number was produced. The pilot protocol stays **UNFROZEN** — its only remaining prerequisite is now the GPU benchmark (`T_sal`/`T_fwd`) plus Compute Gate CG.

### 2026-08-19 | GPU switch REFUSED (insufficient balance); GPU-selection constraint recorded

* Investigated the Lightning environment from the terminal rather than from documentation: Studio `gabriel-allgd-deploy-model-devbox`, teamspace `independentaudioresearch/general`, cluster `lightning-public-prod` (us-east-1), previously `CPU` 4 cores / 15 GB, 387 G persistent disk. `lightning` CLI 2026.06.08 present and already authenticated.
* **Found a hard constraint before spending anything.** The frozen `poetry.lock` pins `torch 1.13.1+cu117`; reading the actual `libtorch_cuda_cu.so` in the `.venv` shows SASS for **sm_70 / sm_75 / sm_80 only**, PTX `compute_86`, with bundled cuBLAS 11.10.3.66 and cuDNN 8.5.0.96. So **only `T4` and `A100` are usable**; `L4`/`L40S`/`RTXP_6000` (sm_89), `H100`/`H200` (sm_90) and `B200` (sm_100) would boot, bill and then fail — and `AGENTS.md` forbids relaxing the pin. Note `torch.cuda.get_arch_list()` returns `[]` on a CPU-only machine, so it cannot be used for this check; the binary must be read.
* Gabriel chose **`T4`** (cheaper than A100; both natively supported). **The switch FAILED — the Lightning API refused it with `{"code":9, "message":"Insufficient balance to start the cloud space, top up and try again"}`.** The Studio never restarted (verified afterwards: status `Running`, machine `CPU`, `torch.cuda.is_available() == False`), so nothing was billed and nothing was lost. The blocker is the balance of the organization `independentaudioresearch` that owns the teamspace, not anything technical: the free CPU machine keeps running, a GPU machine needs a positive balance. The personal teamspace `gabriel-allgd/general` exists but is completely empty (no studios, no jobs) — using it would mean rebuilding the whole environment from GitHub + `fetch_public_artifacts.sh` + `environment_report.md` (~125 GB, several hours, fully scripted). Awaiting Gabriel's decision; the switch must not be retried until a top-up is confirmed.
* Recorded the machine table, the derivation, the VRAM caveat (T4 16 GB may be tight for base-model `(1,2,3,5)` saliency and M4 generation; fallback is `A100`, never an L4) and the `lightning` CLI commands in `docs/environment_report.md`; added a READ-FIRST resume block to `docs/HANDOFF.md` with the ordered unblocked queue.
* **No GPU number was produced and no GPU was attached** — `gpu_benchmark.py` has still never run and `docs/compute_budget.md` remains 100% `TBD_MEASURED`. Compute Gate CG stays unresolved and M3 stays blocked. What changed is that the blocker is now precisely identified and costed: it is a billing action on the Lightning org, plus a verified constraint on which GPU may be chosen afterwards.

### 2026-08-19 | F10 fixed (first upstream patch), backward regression test added, suite 13/13

* Reproduced F10 on CPU before touching anything, as claimed in M1-008: 16 `BasicTransformerBlock` on the real pruned U-Net, all with `checkpoint=True`; forward fine, `backward()` raises.
* **Found the root cause is broader than recorded:** `attention.py:379` defaults `BasicTransformerBlock.checkpoint=True` and checkpoints regardless of the U-Net's `use_checkpoint` flag, which is absent from the config and therefore `False`. So the discarded option (a) (`use_checkpoint=False`) would **not** have fixed F10 — the chosen patch was the only workable option of the three offered.
* Applied the patch decided in DECISION-F10 (filter `input_params` by `requires_grad`, re-expand with `None` to keep the return arity). This is the project's **first deliberate upstream patch**: `git diff upstream-frozen -- audioldm_train/` is no longer empty — 1 file, 16 insertions / 2 deletions, 8 of them the comment justifying it.
* **Added the regression test that never existed** (`test_peft_backward_real_unet.py`, R7a–R7d PASS). R7b is the one that matters: gradients through the checkpointed path equal the non-checkpointed path with **max|Δ| = 0.000e+00** over 568 tensors, which proves correctness rather than just absence of a crash.
* **New finding worth carrying:** on a freshly-initialised U-Net the final conv is `zero_module`-ed, so gradient reaches only 1 of 284 adapters and any backward-based test on a random-init model is nearly vacuous. Real weights give `sum|W| = 174.98` and all 284 receive gradient. R7c asserts both halves so the test cannot be weakened back.
* Verified nothing numerical moved: **R5 bit-exactness still 690/690**, full suite **13/13 modules PASS**.
* Still open and unclaimed: **M1 GPU acceptance** (no optimization step has run on a GPU) and the benchmark. `docs/compute_budget.md` stays 100% `TBD_MEASURED`, Compute Gate CG unresolved, M3 blocked. Per Gabriel's cost instruction the next GPU work goes through `lightning job run --studio ... --machine T4` with the Studio left on free CPU.

### 2026-08-19 | M1 acceptance script written; it found F11 (aliased snapshot); two job failures encoded as rules

* Wrote `scripts/research/m1_gpu_acceptance.py` while the benchmark job ran — CPU work that does not depend on the benchmark numbers, per the frozen policy.
* **It immediately found a real defect, F11.** `training_state_dict` cloned the adapter tensors but handed back the optimizer/EMA `state_dict()` payloads by reference, so continuing to train silently rewrote the "snapshot". Resume on the real U-Net diverged by **max|Δ| = 1.570e-04** across 878 trainable tensors after only 2 replayed steps. Fixed with a deep clone; resume is now **exactly 0.000e+00**.
* **The interesting part is why S3 never caught it:** S3 pickles the state through `torch.save`, and serialisation breaks the aliasing. Only an in-memory snapshot exposes it — which is what a resume test, or a training loop doing periodic in-memory checkpointing, actually does. Added **S4 SNAPSHOT-IMMUTABLE**.
* The same CPU dry run also caught a bug of mine before it could cost a job: `ema.update(unet)` where the EMA had been built on the holder, so every lookup raised `KeyError`.
* **Two GPU jobs failed, ≈0.198 credits total, both fast and both my sequencing errors:** `gpu-benchmark-1` lacked the `cd` into the repo (a job starts in `$HOME`), and `gpu-benchmark-2` was refused by its own dirty-tree preflight guard because I launched with uncommitted edits. The guards worked exactly as designed; the rules are now written down instead of remembered.
* Suite **13/13 PASS**. Still **zero GPU numbers**: `compute_budget.md` remains 100% `TBD_MEASURED`, Compute Gate CG unresolved, M3 blocked, and **M1 GPU acceptance unclaimed**.

### 2026-08-19 | First real GPU measurement: compute_budget populated, CG escalated to a schedule decision

* Lightning Job `gpu-benchmark-3` completed on a Tesla T4 for **0.1372 credits**, at clean commit `e6f50f4`, with the Studio left on free CPU throughout. The project has real GPU numbers for the first time.
* **The F9/F10 fixes hold on CUDA, not just CPU:** the job's own R7a preflight gate passed on the GPU — 284/284 adapters received non-zero gradients, 0 frozen base parameters received any — and the smoke stage reproduced the same census independently.
* **I was wrong about T4 headroom.** I had flagged 16 GB as possibly tight for this workload; measured peak for PEFT training is **4.18 GB, about 29% of the card**, with no OOM at any rung of the 1/2/4/8 ladder. `MAX_STABLE_BATCH=8` is the largest *tested*, not the largest that fits: 10.385 GB were still free and per-sample throughput was still improving. The ladder should be extended to 16/32 before any long run is costed.
* **The compute picture is lopsided in a way that matters scientifically.** Derived through the master plan's own §7.3 formulas: M1 + M2 + M3A + M3B — the entire RQ1/RQ2 modality-swap and paired-saliency programme — comes to **0.82 GPU-hours total**. M5 recovery alone is **46.46 GPU-hours per model**, roughly 50× everything else combined, and M4 generation cannot be projected at all because `Tgen` was never measured.
* **Compute Gate CG therefore stays UNRESOLVED and is Gabriel's call.** Conditions 1 and 2 of §7.4 are satisfied; condition 3 cannot be satisfied as the plan specifies it. The master plan states explicitly that a failed compute gate is a schedule decision, not a scientific failure — and the numbers support that reading: the core contribution is affordable now, and it is the recovery arm and the generation-based evaluation that the gate is really about.
* Two GPU jobs had failed earlier on my own sequencing errors (missing `cd`; dirty tree), ≈0.198 credits, both caught fast by the preflight design and both now encoded as rules.
